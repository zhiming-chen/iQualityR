% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier.R
\name{detect_outliers_mahalanobis}
\alias{detect_outliers_mahalanobis}
\title{Mahalanobis distance outlier detection (multivariate)}
\usage{
detect_outliers_mahalanobis(data, alpha = 0.05)
}
\arguments{
\item{data}{Data frame or matrix (one variable per column)}

\item{alpha}{Significance level (default 0.05)}
}
\value{
List containing outliers (outlier row numbers), mahalanobis_dist (Mahalanobis distance vector),
critical_value, n_outliers
}
\description{
Detect outliers in multivariate data using Mahalanobis distance.
Accounts for correlations between variables.
}
\examples{
set.seed(123)
df <- data.frame(x = rnorm(100), y = rnorm(100))
df <- rbind(df, c(10, 10))
detect_outliers_mahalanobis(df)
}
