% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size.R
\name{sample_size_paired}
\alias{sample_size_paired}
\title{Sample size for paired t-test}
\usage{
sample_size_paired(
  mu0,
  mu1,
  sigma_d,
  alpha = 0.05,
  power = 0.8,
  alternative = c("two.sided", "less", "greater")
)
}
\arguments{
\item{mu0}{Hypothesized mean difference (usually 0).}

\item{mu1}{Alternative mean difference to detect.}

\item{sigma_d}{Standard deviation of the paired differences.}

\item{alpha}{Significance level (default 0.05).}

\item{power}{Desired power (default 0.80).}

\item{alternative}{Alternative hypothesis direction
(\code{"two.sided"}, \code{"less"}, \code{"greater"}).}
}
\value{
A list with \code{n} (number of pairs), \code{actual_power}, \code{effect_size},
and \code{parameters}.
}
\description{
Calculates the required number of paired observations for a paired t-test.
The paired test reduces to a one-sample t-test on the within-pair
differences \code{d = y2 - y1}, so the sample size is driven by the mean
difference \code{delta = mu1 - mu0} relative to the standard deviation of the
differences \code{sigma_d}.
}
\examples{
# Detect a mean difference of 0.5 with SD of differences = 1
sample_size_paired(mu0 = 0, mu1 = 0.5, sigma_d = 1, power = 0.80)
}
