% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iqr_resampling.R
\name{resampling_report}
\alias{resampling_report}
\title{Convenience resampling report function}
\usage{
resampling_report(
  result,
  format = c("data.frame", "console", "excel"),
  path = NULL,
  audience = "manager",
  theme = "academic"
)
}
\arguments{
\item{result}{A \code{stat_result} from \code{\link[=resampling_run]{resampling_run()}} or
\code{ResamplingAnalyzer}.}

\item{format}{Output format: \code{"data.frame"} (default), \code{"console"},
\code{"excel"}.}

\item{path}{File path for \code{format = "excel"}.}

\item{audience}{Audience level for console interpretation.}

\item{theme}{Theme name or \code{IqrTheme} object (for Excel styling).}
}
\value{
For \code{"data.frame"}: a data frame. Otherwise invisible NULL.
}
\description{
Report a \code{stat_result} returned by \code{\link[=resampling_run]{resampling_run()}} without creating an
R6 object. Dispatches on \code{format} to console / data.frame / excel output.
}
\examples{
set.seed(123)
x <- rnorm(50, mean = 100, sd = 5)
result <- resampling_run("bootstrap_ci", statistic = mean,
                         data = x, R = 499, seed = 1)
resampling_report(result, format = "data.frame")
}
