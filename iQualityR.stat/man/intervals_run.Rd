% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iqr_intervals.R
\name{intervals_run}
\alias{intervals_run}
\title{Convenience interval-estimation function}
\usage{
intervals_run(
  interval_type,
  ...,
  plot = FALSE,
  plot_type = "auto",
  interpret = FALSE,
  audience = "manager",
  theme = "academic"
)
}
\arguments{
\item{interval_type}{Interval type code (see \link{iqr_intervals}).}

\item{...}{Parameters forwarded to \code{IntervalAnalyzer$analyze()}.}

\item{plot}{Logical; print a plot immediately.}

\item{plot_type}{Plot type forwarded to \code{intervals_plot()}.}

\item{interpret}{Logical; print an interpretation immediately.}

\item{audience}{Audience level for the interpretation.}

\item{theme}{Theme name or \code{IqrTheme} object.}
}
\value{
A \code{stat_result} S3 object (invisibly).
}
\description{
Execute an interval estimation without creating an R6 object. Returns a
\code{stat_result} S3 object so the caller can still feed it to
\code{intervals_plot()} / \code{intervals_interpret()} / \code{intervals_report()}.
}
\examples{
set.seed(123)
x <- rnorm(30, mean = 100, sd = 5)
result <- intervals_run("ci_mean", x = x)
print(result)
}
