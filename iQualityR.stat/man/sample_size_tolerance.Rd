% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size.R
\name{sample_size_tolerance}
\alias{sample_size_tolerance}
\title{Sample size driven by a normal tolerance interval}
\usage{
sample_size_tolerance(p, conf_level, max_half_width, sigma = 1)
}
\arguments{
\item{p}{Content (population coverage) of the tolerance interval, in \verb{(0,1)}.}

\item{conf_level}{Confidence level (probability that the interval covers at
least proportion \code{p} of the population), in \verb{(0,1)}.}

\item{max_half_width}{Multiple of \code{sigma} that the interval half-width
(\code{k * s}) must not exceed. For example 3.0 means \code{k * s <= 3 * sigma}.}

\item{sigma}{Anticipated population SD (used only to express the width
constraint; the returned \code{n} is scale-free).}
}
\value{
A list with \code{n}, \code{k_factor}, \code{half_width} (in units of \code{sigma}),
\code{p}, \code{conf_level}, and \code{parameters}.
}
\description{
Calculates the minimum sample size so that a two-sided normal-theory
tolerance interval \verb{x_bar +/- k * s} has the required content \code{p} and
confidence \code{conf_level}, while keeping the interval half-width no larger
than \code{max_half_width * sigma}.

The k-factor for a two-sided normal tolerance interval is approximated
using the Howe (1969) / exact chi-square approach:
\verb{k = sqrt( (nu * (1 + 1/n) * z_p^2) / chi2_\{1-gamma, nu\} )}
where \code{nu = n - 1}, \verb{z_p = z_\{(1+p)/2\}}, and \verb{chi2_\{1-gamma, nu\}} is the
\code{1-gamma} quantile of chi-square with \code{nu} df.
}
\examples{
sample_size_tolerance(p = 0.95, conf_level = 0.95, max_half_width = 3.0)
}
