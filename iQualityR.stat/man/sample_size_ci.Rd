% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size.R
\name{sample_size_ci}
\alias{sample_size_ci}
\title{Sample size driven by confidence-interval width}
\usage{
sample_size_ci(
  type = c("mean", "proportion"),
  h,
  sigma = NULL,
  p = 0.5,
  conf_level = 0.95
)
}
\arguments{
\item{type}{\code{"mean"} or \code{"proportion"}.}

\item{h}{Desired half-width of the two-sided CI (the full width is \code{2*h}).}

\item{sigma}{Population SD (required when \code{type = "mean"}).}

\item{p}{Anticipated proportion in \verb{(0, 1)} (required when \code{type = "proportion"}).
Defaults to 0.5 (maximises required n) when omitted.}

\item{conf_level}{Confidence level (default 0.95).}
}
\value{
A list with \code{n}, \code{half_width}, \code{full_width}, \code{conf_level}, and
\code{parameters}.
}
\description{
Calculates the sample size required so that a two-sided confidence interval
has at most a specified half-width (precision). Supports the mean (normal,
known or estimated sigma) and a proportion.

For the mean:  \code{n = (z * sigma / h)^2} where \code{h} is the desired half-width.
For the proportion:  \code{n = z^2 * p*(1-p) / h^2}.
}
\examples{
sample_size_ci(type = "mean", h = 0.3, sigma = 1, conf_level = 0.95)
sample_size_ci(type = "proportion", h = 0.05, p = 0.5, conf_level = 0.95)
}
