% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iqr_resampling.R
\name{resampling_run}
\alias{resampling_run}
\title{Convenience resampling function}
\usage{
resampling_run(
  resample_type,
  ...,
  plot = FALSE,
  plot_type = "auto",
  interpret = FALSE,
  audience = "manager",
  theme = "academic"
)
}
\arguments{
\item{resample_type}{Resample type code (see \link{iqr_resampling}).}

\item{...}{Parameters forwarded to \code{ResamplingAnalyzer$analyze()}.}

\item{plot}{Logical; print a plot immediately.}

\item{plot_type}{Plot type forwarded to \code{resampling_plot()}.}

\item{interpret}{Logical; print an interpretation immediately.}

\item{audience}{Audience level for the interpretation.}

\item{theme}{Theme name or \code{IqrTheme} object.}
}
\value{
A \code{stat_result} S3 object (invisibly).
}
\description{
Execute a resampling procedure without creating an R6 object. Returns a
\code{stat_result} S3 object so the caller can still feed it to
\code{resampling_plot()} / \code{resampling_interpret()} / \code{resampling_report()}.
}
\examples{
set.seed(123)
x <- rnorm(50, mean = 100, sd = 5)
result <- resampling_run("bootstrap_ci", statistic = median,
                         data = x, R = 999, seed = 1)
print(result)
}
