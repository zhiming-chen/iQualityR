% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size.R
\name{sample_size_reliability}
\alias{sample_size_reliability}
\title{Sample size for a reliability / success-run study}
\usage{
sample_size_reliability(reliability, conf_level, n_failures = 0L)
}
\arguments{
\item{reliability}{Target reliability (probability of a unit surviving),
in \verb{(0, 1)}.}

\item{conf_level}{Confidence level (probability of passing the test when
true reliability is below target), in \verb{(0, 1)}.}

\item{n_failures}{Number of failures allowed in the test (default 0).}
}
\value{
A list with \code{n}, \code{reliability}, \code{conf_level}, \code{n_failures}, and
\code{parameters}.
}
\description{
Calculates the minimum sample size for a reliability demonstration test
under a binomial (success-run) model. With zero allowed failures the
required sample size for demonstrating reliability \code{R} at confidence level
\code{conf_level} is \code{n = log(1 - conf_level) / log(R)}. When \code{n_failures > 0}
failures are allowed, the sample size is found by solving the binomial
cumulative probability \code{P(X <= n_failures | n, 1 - R) <= 1 - conf_level}.
}
\examples{
# Demonstrate 95\% reliability with 90\% confidence, 0 failures
sample_size_reliability(reliability = 0.95, conf_level = 0.90)
# Allow 1 failure
sample_size_reliability(reliability = 0.95, conf_level = 0.90, n_failures = 1)
}
