% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iqr_resampling.R
\name{resampling_plot}
\alias{resampling_plot}
\title{Convenience resampling plotting function}
\usage{
resampling_plot(
  result,
  plot_type = "auto",
  show_table = FALSE,
  theme = "academic"
)
}
\arguments{
\item{result}{A \code{stat_result} from \code{\link[=resampling_run]{resampling_run()}} or
\code{ResamplingAnalyzer}.}

\item{plot_type}{One of \code{"auto"}, \code{"hist"}, \code{"density"}, \code{"qq"}.}

\item{show_table}{Logical; annotate observed statistic / CI / p-value.}

\item{theme}{Theme name or \code{IqrTheme} object.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot a \code{stat_result} returned by \code{\link[=resampling_run]{resampling_run()}} (or by
\code{ResamplingAnalyzer} directly).
}
\examples{
if (requireNamespace("iQualityR.plot", quietly = TRUE)) {
  set.seed(123)
  x <- rnorm(50, mean = 100, sd = 5)
  result <- resampling_run("bootstrap_ci", statistic = mean,
                           data = x, R = 499, seed = 1)
  resampling_plot(result, plot_type = "hist")
}
}
