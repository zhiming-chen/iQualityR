% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sigma_estimate.R
\name{sigma_decomposition}
\alias{sigma_decomposition}
\title{Standard Deviation Decomposition}
\usage{
sigma_decomposition(
  x,
  subgroup = NULL,
  n_size = NULL,
  within_method = "r_bar",
  between_method = "mssd",
  total_unbiased = FALSE,
  within_unbiased = TRUE,
  between_unbiased = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector or data frame containing measurements}

\item{subgroup}{Optional vector specifying subgroup membership}

\item{n_size}{Optional integer for sequential subgroup size}

\item{within_method}{Method for within-group standard deviation estimation, default is "r_bar"}

\item{between_method}{Method for between-group standard deviation estimation, default is "mssd"}

\item{total_unbiased}{Whether to use unbiased estimation for total standard deviation, default is FALSE}

\item{within_unbiased}{Whether to use unbiased estimation for within-group standard deviation, default is TRUE}

\item{between_unbiased}{Whether to use unbiased estimation for between-group standard deviation, default is TRUE}

\item{...}{Additional parameters passed to sigma_estimate}
}
\value{
A data frame containing within-group, between-group, combined, and total standard deviations
}
\description{
Decomposes total standard deviation into within-group and between-group components
}
\examples{
# Generate data with within-group and between-group variation
set.seed(123)
between_var <- rnorm(10, mean = 0, sd = 1.5)
data <- unlist(lapply(between_var, function(x) rnorm(5, mean = x, sd = 1)))
subgroup <- rep(1:10, each = 5)

# Decompose standard deviation
sigma_decomp <- sigma_decomposition(data, subgroup = subgroup)
print(sigma_decomp)
}
