% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size.R
\name{sample_size_regression}
\alias{sample_size_regression}
\title{Sample size for linear regression (global F-test / R²)}
\usage{
sample_size_regression(p, r_squared, alpha = 0.05, power = 0.8)
}
\arguments{
\item{p}{Number of predictor variables (excluding the intercept).}

\item{r_squared}{Anticipated population R² (0 < r_squared < 1).}

\item{alpha}{Significance level (default 0.05).}

\item{power}{Desired power (default 0.80).}
}
\value{
A list with \code{n} (total sample size, >= p + 2), \code{actual_power},
\code{f_squared}, \code{r_squared}, and \code{parameters}.
}
\description{
Calculates the required sample size to detect a given population R² in a
linear regression with \code{p} predictors, using the global F-test. The effect
size is Cohen's f² = R² / (1 - R²). The calculation uses an iterative
search over the noncentral F distribution so that the achieved power
matches the target.
}
\examples{
# 3 predictors, R² = 0.30
sample_size_regression(p = 3, r_squared = 0.30, power = 0.80)
}
