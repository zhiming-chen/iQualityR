% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size.R
\name{odds_ratio}
\alias{odds_ratio}
\title{Odds ratio for a 2x2 table}
\usage{
odds_ratio(a, b, c, d, conf_level = 0.95)
}
\arguments{
\item{a}{Count of exposed+ / outcome+ cells.}

\item{b}{Count of exposed+ / outcome- cells.}

\item{c}{Count of exposed- / outcome+ cells.}

\item{d}{Count of exposed- / outcome- cells.}

\item{conf_level}{Confidence level (default 0.95).}
}
\value{
A list with elements \code{odds_ratio}, \code{conf.int}, \code{conf.level},
\code{log_or}, \code{se_log_or}, \code{method}, and \code{corrected} (logical; whether the
zero-cell correction was applied).
}
\description{
Computes the odds ratio (OR) for a 2x2 contingency table, with a Wald-type
confidence interval on the log scale. A Haldane-Anscombe correction
(add 0.5 to every cell) is applied automatically when any cell is zero so
the estimate remains finite.

The 2x2 table layout is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{             Outcome+  Outcome-
Exposed+       a         b
Exposed-       c         d
}\if{html}{\out{</div>}}

OR = (a/b) / (c/d) = (a\emph{d) / (b}c).
}
\examples{
# Case-control: 45 exposed cases, 25 exposed controls,
# 20 unexposed cases, 60 unexposed controls
odds_ratio(a = 45, b = 25, c = 20, d = 60)
}
