% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reliability_functions.R
\name{cox_model}
\alias{cox_model}
\title{Cox Proportional Hazards Model}
\usage{
cox_model(data, time_var, status_var, factors, conf_level = 0.95, ...)
}
\arguments{
\item{data}{Data frame.}

\item{time_var}{Character. Name of the time column.}

\item{status_var}{Character. Name of the status column.}

\item{factors}{Character vector. Covariate names for the model.}

\item{conf_level}{Numeric. Confidence level.}

\item{...}{Additional arguments passed to \link{ReliabilityPlan}$initialize().}
}
\value{
List of analysis results.
}
\description{
Convenience wrapper for fitting a Cox proportional hazards
model. Requires the \code{survival} package.
}
\examples{
\donttest{
if (requireNamespace("survival", quietly = TRUE)) {
  set.seed(42)
  data <- data.frame(
    time   = rexp(100, rate = 0.01),
    status = rbinom(100, 1, 0.7),
    age    = rnorm(100, 50, 10),
    treat  = rbinom(100, 1, 0.5)
  )
  result <- cox_model(data, "time", "status", factors = c("age", "treat"))
  result$cox_model$coefficients
}
}
}
