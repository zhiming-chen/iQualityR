% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{create_task_registry}
\alias{create_task_registry}
\title{Create Task Registry}
\usage{
create_task_registry(
  tasks,
  analyzer_suffix = "Analyzer",
  plotter_suffix = "Plotter",
  reporter_suffix = "Reporter",
  name_transformer = function(x) {
     paste0(toupper(substr(x, 1, 1)),
    tolower(substr(x, 2, nchar(x))))
 }
)
}
\arguments{
\item{tasks}{Character vector of task names.}

\item{analyzer_suffix}{Suffix for analyzer class names (default "Analyzer").}

\item{plotter_suffix}{Suffix for plotter class names (default "Plotter").}

\item{reporter_suffix}{Suffix for reporter class names (default "Reporter").}

\item{name_transformer}{Optional function to transform task names to class names.
Defaults to capitalizing the first letter and lowercasing the rest.}
}
\value{
A named list; each element is itself a list with \code{analyzer},
\code{plotter}, and \code{reporter} character entries.
}
\description{
Create a registry mapping task names to analyzer, plotter, and reporter class names.
Useful for dynamic dispatch of task-specific logic based on naming convention.
}
