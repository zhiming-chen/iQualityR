% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/type1_functions.R
\name{iqr_linearity_bias}
\alias{iqr_linearity_bias}
\title{Quick Gage Linearity & Bias Study}
\usage{
iqr_linearity_bias(
  data,
  reference_values = NULL,
  lsl = NULL,
  usl = NULL,
  tolerance = NULL,
  natural_zero = FALSE,
  process_variation = "from_study",
  historical_sd = NULL,
  resolution = NULL,
  u_cal = 0,
  u_lin = NULL,
  u_rest = list(),
  linearity_corrected = FALSE,
  conf_level = 0.95,
  theme = "academic"
)
}
\arguments{
\item{data}{Data.frame with 'reference' and 'measurement' columns (long
format) OR a data.frame/matrix whose columns are reference values (wide
format). Wide format is auto-converted to long via \code{data.table::melt()}.}

\item{reference_values}{Numeric vector of reference values (optional, auto-detected)}

\item{lsl}{Lower specification limit}

\item{usl}{Upper specification limit}

\item{tolerance}{Numeric, tolerance directly (T). If given, overrides lsl/usl.}

\item{natural_zero}{Logical, if TRUE and \code{usl} is given, tolerance = usl - 0.}

\item{process_variation}{Numeric, process variation (PV) for linearity
calculations. See description for accepted values.}

\item{historical_sd}{Numeric, historical sigma for
\code{process_variation = "from_historical_sigma"} mode.}

\item{resolution}{Numeric, measurement system resolution for VDA5 (optional)}

\item{u_cal}{Numeric, calibration uncertainty for VDA5 (default 0)}

\item{u_lin}{Numeric, linearity uncertainty for VDA5. Default NULL lets the
analyzer compute u_LIN = |Linearity|/sqrt(3) per VDA 5 §5.3.4.}

\item{u_rest}{List, additional uncertainty factors for VDA5 (optional)}

\item{linearity_corrected}{Logical, if TRUE the gage's linearity is software
compensated in production and u_LIN falls back to s_regression (VDA 5 §5.3.4).}

\item{conf_level}{Confidence level}

\item{theme}{Theme name}
}
\value{
Type1Task object
}
\description{
Quick entry for multiple reference value linearity & bias study.

Process Variation (PV) is required by Minitab's Gage Linearity and Bias
Study for Linearity = |slope|*PV and \%Linearity/\%Bias calculations.
\code{process_variation} accepts:
\itemize{
\item numeric scalar (typically 6*sigma from a capability study)
\item \code{"from_study"}: use 6*s_pooled(within-reference) with a warning
\item \code{"from_historical_sigma"}: use 6*historical_sd (requires \code{historical_sd})
\item \code{NULL}: degrades to \code{"from_study"} with a warning (E3 degradation
strategy - do not hard-stop the analysis)
}
}
