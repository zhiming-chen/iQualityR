% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Capability_nonparametric.R
\name{capability_nonparametric}
\alias{capability_nonparametric}
\title{Non-Parametric Process Capability Analysis}
\usage{
capability_nonparametric(
  data,
  measurement,
  lsl,
  usl,
  target = NULL,
  conf_level = 0.95,
  use_bootstrap = FALSE,
  bootstrap_samples = 1000,
  nonparametric_method = "kernel",
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name (character).}

\item{lsl}{Lower specification limit (numeric).}

\item{usl}{Upper specification limit (numeric).}

\item{target}{Optional target value (numeric).}

\item{conf_level}{Confidence level (default 0.95).}

\item{use_bootstrap}{Logical; whether to use Bootstrap for confidence intervals
(default FALSE).}

\item{bootstrap_samples}{Number of Bootstrap replications (default 1000).}

\item{nonparametric_method}{Non-parametric method: \code{"kernel"} (kernel density
estimation) or \code{"empirical"} (empirical distribution).}

\item{theme}{Theme name or \link[iQualityR.core:IqrTheme]{iQualityR.core::IqrTheme} object (default "academic").}

\item{...}{Additional arguments passed to \link{IqrCapabilityTask}.}
}
\value{
An \link{IqrCapabilityTask} object with computed results.
}
\description{
Performs non-parametric process capability analysis based on the empirical
distribution. Suitable when the underlying distribution is unknown or does
not fit common parametric families.
}
\examples{
set.seed(123)
\donttest{
data <- data.frame(measurement = rweibull(100, shape = 2, scale = 50))

# Basic non-parametric capability analysis
result <- capability_nonparametric(
  data = data, measurement = "measurement",
  lsl = 10, usl = 100
)
result$summary()

# Using Bootstrap confidence intervals
result <- capability_nonparametric(
  data = data, measurement = "measurement",
  lsl = 10, usl = 100,
  use_bootstrap = TRUE, bootstrap_samples = 500
)
result$summary()

# Specify the non-parametric method
result <- capability_nonparametric(
  data = data, measurement = "measurement",
  lsl = 10, usl = 100,
  nonparametric_method = "empirical"
)
result$summary()
}
}
