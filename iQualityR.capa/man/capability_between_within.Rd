% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Capability_between_within.R
\name{capability_between_within}
\alias{capability_between_within}
\title{Between/Within Process Capability Analysis}
\usage{
capability_between_within(
  data,
  measurement,
  lsl,
  usl,
  target = NULL,
  subgroup,
  conf_level = 0.95,
  use_bootstrap = FALSE,
  bootstrap_samples = 1000,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name (character).}

\item{lsl}{Lower specification limit (numeric).}

\item{usl}{Upper specification limit (numeric).}

\item{target}{Optional target value used for Cpm calculation (numeric).}

\item{subgroup}{Subgroup column name for within/between sigma decomposition
(required).}

\item{conf_level}{Confidence level (default 0.95).}

\item{use_bootstrap}{Logical; whether to compute bootstrap confidence intervals.}

\item{bootstrap_samples}{Number of bootstrap replications (default 1000).}

\item{theme}{Theme name or \link[iQualityR.core:IqrTheme]{iQualityR.core::IqrTheme} object (default "academic").}

\item{...}{Additional arguments passed to \link{IqrCapabilityTask}.}
}
\value{
An \link{IqrCapabilityTask} object with computed results. In addition to
the standard \code{sd_within} and \code{sd_overall} statistics, the result container
also exposes \code{sd_between} and \code{sd_between_within}.
}
\description{
Computes process capability indices under the Minitab "Between/Within"
(B/W) convention. Cp/Cpk (B/W) use the combined between-within sigma
(\code{sigma_between_within}) and Pp/Ppk use the overall sigma (\code{sigma_total}).

Requires subgroup data to decompose sigma into within, between, and
between-within components via \link[iQualityR.stat:sigma_decomposition]{iQualityR.stat::sigma_decomposition}. This
path is appropriate when both within-subgroup and between-subgroup variation
should be pooled for the short-term (potential) capability estimate, which
gives a more honest Cp/Cpk than the pure within-sigma approach when the
process drifts between subgroups.
}
\examples{
set.seed(123)
\donttest{
data <- data.frame(
  measurement = rnorm(100, mean = 50, sd = 2),
  subgroup = rep(1:20, each = 5)
)
result <- capability_between_within(
  data = data, measurement = "measurement",
  lsl = 44, usl = 56, target = 50,
  subgroup = "subgroup"
)
result$summary()
}
}
