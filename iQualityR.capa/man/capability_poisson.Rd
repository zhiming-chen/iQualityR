% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AttributeCapabilityPlan.R
\name{capability_poisson}
\alias{capability_poisson}
\title{Poisson process capability analysis}
\usage{
capability_poisson(
  data,
  defects,
  sample_sizes,
  target_rate = NULL,
  z_shift = 1.5,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{defects}{Column name for the count of defects per subgroup.}

\item{sample_sizes}{Column name for the exposure (units / opportunities)
per subgroup.}

\item{target_rate}{Optional target maximum defects per unit.}

\item{z_shift}{Sigma shift convention (default 1.5, Six Sigma).}

\item{conf_level}{Confidence level.}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrAttributeCapabilityTask} object (invisibly).
}
\description{
Performs attribute capability analysis for defect count data under the
Poisson assumption. Estimates the rate \code{u} (defects per unit), computes
an exact (Garwood) CI, DPMO, Z.Bench / sigma level, a u-chart for
stability, and a dispersion test for the Poisson assumption. This is
the Minitab "Poisson Capability Analysis" equivalent.
}
\examples{
\dontrun{
df <- data.frame(roll = 1:30, defects = rpois(30, 2),
                 meters = rep(50, 30))
task <- capability_poisson(data = df, defects = "defects",
                           sample_sizes = "meters",
                           target_rate = 0.05)
task$summary()
task$plot(type = "full")
}
}
