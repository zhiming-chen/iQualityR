% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Capability_normal.R
\name{capability_normal}
\alias{capability_normal}
\title{Normal Process Capability Analysis}
\usage{
capability_normal(
  data,
  measurement,
  lsl,
  usl,
  target = NULL,
  subgroup = NULL,
  sixpack = FALSE,
  conf_level = 0.95,
  transform = NULL,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name (character).}

\item{lsl}{Lower specification limit (numeric).}

\item{usl}{Upper specification limit (numeric).}

\item{target}{Optional target value used for Cpm calculation (numeric).}

\item{subgroup}{Optional subgroup column name for within-group sigma estimation.}

\item{sixpack}{Logical; whether to generate the Sixpack diagnostic plots.}

\item{conf_level}{Confidence level (default 0.95).}

\item{transform}{Optional normality transform for the normal path:
\code{"box_cox"}, \code{"johnson"}, \code{"auto"}, or \code{NULL} (default, no transform).}

\item{theme}{Theme name or \link[iQualityR.core:IqrTheme]{iQualityR.core::IqrTheme} object (default "academic").}

\item{...}{Additional arguments passed to \link{IqrCapabilityTask}.}
}
\value{
An \link{IqrCapabilityTask} object with computed results.
}
\description{
Computes process capability indices (Cp, Cpk, Pp, Ppk, and optionally Cpm)
under the normal distribution assumption, and optionally generates a Sixpack
diagnostic plot set.

When \code{transform} is set, the data (and the specification limits, following
Minitab convention) are transformed using the chosen method
(\code{"box_cox"}, \code{"johnson"}, or \code{"auto"}) before normal capability indices
are computed. The transform parameters and normality diagnostics
(before vs. after) are surfaced through \code{task$results$diagnostics}.
}
\examples{
set.seed(123)
\donttest{
data <- data.frame(
  measurement = rnorm(100, mean = 50, sd = 2),
  subgroup = rep(1:20, each = 5)
)
result <- capability_normal(
  data = data, measurement = "measurement",
  lsl = 44, usl = 56, target = 50,
  subgroup = "subgroup"
)
result$summary()
}
}
