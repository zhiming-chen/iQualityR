% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Capability_nonnormal.R
\name{capability_nonnormal}
\alias{capability_nonnormal}
\title{Non-Normal Process Capability Analysis}
\usage{
capability_nonnormal(
  data,
  measurement,
  lsl,
  usl,
  distribution = "auto",
  target = NULL,
  conf_level = 0.95,
  use_bootstrap = FALSE,
  bootstrap_samples = 1000,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name (character).}

\item{lsl}{Lower specification limit (numeric).}

\item{usl}{Upper specification limit (numeric).}

\item{distribution}{Distribution name or \code{"auto"} for automatic selection
(default \code{"auto"}). Available distributions: \code{"weibull"}, \code{"lognormal"},
\code{"gamma"}, \code{"exponential"}, \code{"logistic"}, \code{"beta"}.}

\item{target}{Optional target value used for Cpm calculation (numeric).}

\item{conf_level}{Confidence level (default 0.95).}

\item{use_bootstrap}{Logical; whether to use Bootstrap for confidence intervals.}

\item{bootstrap_samples}{Number of Bootstrap replications (default 1000).}

\item{theme}{Theme name or \link[iQualityR.core:IqrTheme]{iQualityR.core::IqrTheme} object (default "academic").}

\item{...}{Additional arguments passed to \link{IqrCapabilityTask}.}
}
\value{
An \link{IqrCapabilityTask} object with computed results.
}
\description{
Performs process capability analysis under a non-normal distribution
assumption. The distribution can be auto-selected or specified explicitly.
Supported distributions: Weibull, Lognormal, Gamma, Exponential, Logistic,
and Beta.
}
\examples{
set.seed(123)
\donttest{
data <- data.frame(
  measurement = rweibull(100, shape = 2, scale = 50)
)
result <- capability_nonnormal(
  data = data, measurement = "measurement",
  lsl = 10, usl = 100,
  distribution = "auto"
)
result$summary()
}
}
