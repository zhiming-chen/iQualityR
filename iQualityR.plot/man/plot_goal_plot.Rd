% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_capability_goalplot.R
\name{plot_goal_plot}
\alias{plot_goal_plot}
\title{Goal Plot (JMP-style)}
\usage{
plot_goal_plot(
  processes,
  shift_limit = 0.5,
  spread_limit = 0.5,
  label_points = TRUE,
  theme = NULL
)
}
\arguments{
\item{processes}{data.frame with one row per process and columns:
\code{name} (character), \code{mean} (numeric), \code{sd} (numeric, overall sigma),
\code{target} (numeric), \code{lsl} (numeric), \code{usl} (numeric).}

\item{shift_limit}{Numeric scalar, goal-box threshold on the shift axis
(fraction of half-tolerance). Default 0.5 (mean within a quarter of the
spec width from target, i.e. |mean - target| <= 0.25 * (USL - LSL)).}

\item{spread_limit}{Numeric scalar, goal-box threshold on the spread axis
(fraction of half-tolerance). Default 0.5 (3sigma <= 0.25 * (USL - LSL),
i.e. Cp >= 2).}

\item{label_points}{Logical; if TRUE, label each point with its process
name. Default TRUE.}

\item{theme}{Theme spec.}
}
\value{
A ggplot object.
}
\description{
Draws the JMP-standard Goal Plot for comparing many processes on a single
view. Each point is one process (CTQ). The X-axis is the mean shift from
target as a fraction of the half-tolerance (0 = on target, 1 = mean at a
spec limit). The Y-axis is the process spread (3*sigma) as a fraction of
the half-tolerance (0 = no variation, 1 = 3sigma reaches the spec limit,
i.e. Cp = 1). The lower-left "goal box" (shift <= \code{shift_limit} AND
spread <= \code{spread_limit}) marks capable processes.
}
\details{
This is the academic / JMP industry standard for multi-CTQ capability
triage: it separates "off-target" problems (high X) from "too much
variation" problems (high Y) at a glance, which the usual Cpk bar cannot.
}
