% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_factory.R
\name{plot_qq}
\alias{plot_qq}
\title{Quantile-Quantile (QQ) Plot with Goodness-of-Fit Test}
\usage{
plot_qq(
  data,
  sample_col,
  theme = NULL,
  distribution = qnorm,
  dist_params = list(),
  dist_family = c("none", "norm", "unif", "exp", "weibull"),
  qq_line = c("quartiles", "identity"),
  add_confidence = FALSE,
  conf_level = 0.95,
  n_sim = 1000,
  add_test = TRUE,
  test_method = c("auto", "ad", "ks"),
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{sample_col}{Name of the column containing the sample data.}

\item{theme}{Optional theme object from \code{as_iqr_theme()}.}

\item{distribution}{Distribution function for quantiles (e.g., \code{qnorm}, \code{qunif}).
Ignored if \code{dist_family} is specified.}

\item{dist_params}{List of parameters for the distribution (e.g., \code{list(mean=0, sd=1)}).}

\item{dist_family}{Shortcut for common distributions: "none", "norm", "unif", "exp", "weibull".
Overrides \code{distribution} if provided.}

\item{qq_line}{Type of reference line: "quartiles" (line through Q1-Q3, default) or
"identity" (y = x, only meaningful when distribution parameters are known).}

\item{add_confidence}{If \code{TRUE}, adds a simulated confidence band (default \code{FALSE}).}

\item{conf_level}{Confidence level for the band (default 0.95).}

\item{n_sim}{Number of simulations for the confidence band (default 1000).}

\item{add_test}{If \code{TRUE}, adds goodness-of-fit test annotation on the plot.}

\item{test_method}{Character: "ks" (Kolmogorov-Smirnov, general) or "shapiro" (only for normal).}

\item{...}{Additional arguments passed to \code{ggplot2::labs()}.}
}
\value{
A ggplot2 object.
}
\description{
Quantile-Quantile (QQ) Plot with Goodness-of-Fit Test
}
\examples{
\dontrun{
plot_qq(mtcars, "mpg", dist_family = "norm")
plot_qq(mtcars, "mpg", distribution = qexp, dist_params = list(rate = 0.1))
plot_qq(mtcars, "mpg", dist_family = "norm", add_confidence = TRUE)
}
}
