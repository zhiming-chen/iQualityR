% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_stats_table.R
\name{compose_table_plot}
\alias{compose_table_plot}
\title{Compose a Table Grob and One or More ggplot Objects}
\usage{
compose_table_plot(
  table,
  plots,
  direction = c("vertical", "horizontal"),
  ratios = NULL,
  theme = NULL,
  title = NULL,
  subtitle = NULL
)
}
\arguments{
\item{table}{A \code{tableGrob} object (from \code{create_stats_table}).}

\item{plots}{A single ggplot object or a \code{list} of ggplot objects.}

\item{direction}{Character: \code{"vertical"} (table on top, plots below) or
\code{"horizontal"} (table on left, plots on right).}

\item{ratios}{Numeric vector of length 2 giving the relative size of
\code{table} vs \code{plots} (default \code{c(1, 3)} for vertical,
\code{c(1, 4)} for horizontal).}

\item{theme}{Theme spec (only used to resolve annotation title colour).}

\item{title}{Optional overall title for the composed patchwork.}

\item{subtitle}{Optional overall subtitle.}
}
\value{
A \code{patchwork} object.
}
\description{
Patchwork-based layout helper for the "Minitab-style table + chart" pattern.
Places a \code{tableGrob} (from \code{\link{create_stats_table}}) above or
beside one or more ggplot objects at a configurable height/width ratio.
}
\examples{
\dontrun{
g <- create_stats_table(df)
p <- plot_capability_histogram(...)
compose_table_plot(g, p, direction = "vertical", ratios = c(1, 4))
}
}
