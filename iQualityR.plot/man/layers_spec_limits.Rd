% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_factory.R
\name{layers_spec_limits}
\alias{layers_spec_limits}
\title{Create Layers for Specification Limits}
\usage{
layers_spec_limits(
  lsl = NULL,
  usl = NULL,
  theme = NULL,
  spec_color = NULL,
  lsl_label = paste("LSL =", .fmt_spec(lsl)),
  usl_label = paste("USL =", .fmt_spec(usl)),
  orientation = c("v", "h"),
  vline_args = list(),
  annotate_args = list()
)
}
\arguments{
\item{lsl}{Lower specification limit. If NULL, not drawn.}

\item{usl}{Upper specification limit. If NULL, not drawn.}

\item{theme}{Theme spec (NULL / string / function / IqrTheme). Used to
resolve \code{spec_color} when it is NULL. The color is taken from the
theme's semantic palette slot \code{"fail"}.}

\item{spec_color}{Color for specification lines and labels. If NULL
(default), the color is resolved from \code{theme} via
\code{theme_obj$get_pal("semantic", name="fail")}, so the lines follow
the active theme automatically. Pass an explicit color to override.}

\item{lsl_label}{Label for LSL.}

\item{usl_label}{Label for USL.}

\item{orientation}{Direction of the spec lines. \code{"v"} (default) draws
vertical lines via \code{geom_vline} — appropriate when the spec applies
to the x-axis variable (e.g. histograms). \code{"h"} draws horizontal
lines via \code{geom_hline} — appropriate when the spec applies to the
y-axis variable (e.g. individual values charts, trend charts).}

\item{vline_args}{List of arguments passed to geom_vline/geom_hline.}

\item{annotate_args}{List of arguments passed to annotate (applies to both).}
}
\value{
A list of ggplot2 layers.
}
\description{
Create Layers for Specification Limits
}
