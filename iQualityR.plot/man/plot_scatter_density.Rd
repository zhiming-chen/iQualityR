% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_scatter_ok.R
\name{plot_scatter_density}
\alias{plot_scatter_density}
\title{Create Scatter Plot with Density Handling}
\usage{
plot_scatter_density(
  data,
  x_var,
  y_var,
  method = c("alpha", "jitter", "bins", "hex", "density"),
  point_alpha = 0.5,
  point_size = 2,
  jitter_width = 0.1,
  jitter_height = 0.1,
  bins = 30,
  color_low = NULL,
  color_high = NULL,
  add_smooth = FALSE,
  smooth_method = "loess",
  theme = NULL,
  title = NULL,
  subtitle = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame containing the data.}

\item{x_var}{Name of the x-axis variable.}

\item{y_var}{Name of the y-axis variable.}

\item{method}{Method to handle overlapping: "alpha" (transparency), "jitter" (offset),
"bins" (2D binning), "hex" (hexagonal binning), or "density" (density coloring) (default: "alpha").}

\item{point_alpha}{Alpha transparency for points (default: 0.5, used for "alpha" method).}

\item{point_size}{Size for points (default: 2).}

\item{jitter_width}{Width of jitter offset (default: 0.1, used for "jitter" method).}

\item{jitter_height}{Height of jitter offset (default: 0.1, used for "jitter" method).}

\item{bins}{Number of bins for 2D binning (default: 30, used for "bins" method).}

\item{color_low}{Color for low density. Defaults to the theme soft surface color.}

\item{color_high}{Color for high density. Defaults to the theme primary color.}

\item{add_smooth}{If TRUE, adds a smooth line (default: FALSE).}

\item{smooth_method}{Smoothing method: "loess", "gam", or "lm" (default: "loess").}

\item{theme}{Theme to use.}

\item{title}{Chart title.}

\item{subtitle}{Chart subtitle.}

\item{...}{Additional arguments passed to ggplot2 functions.}
}
\value{
A ggplot object.
}
\description{
Creates a scatter plot with various methods to handle overlapping points (overplotting).
Supports alpha blending, jittering, and 2D binning for high-density data.
}
\examples{
# High-density scatter plot with alpha blending
data <- data.frame(
  x = rnorm(1000),
  y = rnorm(1000) + 0.5 * rnorm(1000)
)
plot_scatter_density(data, "x", "y", method = "alpha")

# With jittering
plot_scatter_density(data, "x", "y", method = "jitter")

# With 2D binning
plot_scatter_density(data, "x", "y", method = "bins")

}
