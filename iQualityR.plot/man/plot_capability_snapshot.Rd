% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_capability_goalplot.R
\name{plot_capability_snapshot}
\alias{plot_capability_snapshot}
\title{Process Capability Snapshot (JMP-style)}
\usage{
plot_capability_snapshot(
  values_list,
  processes,
  bins = 20,
  ncol = 3,
  theme = NULL
)
}
\arguments{
\item{values_list}{List of numeric vectors, one per process (the raw
measurements).}

\item{processes}{data.frame with one row per process and columns:
\code{name} (character), \code{target} (numeric), \code{lsl} (numeric),
\code{usl} (numeric), \code{cpk} (numeric, optional — when missing it is
omitted from the strip header).}

\item{bins}{Integer, histogram bin count. Default 20.}

\item{ncol}{Integer, number of columns in the facet grid. Default 3.}

\item{theme}{Theme spec.}
}
\value{
A ggplot object.
}
\description{
Draws a faceted small-multiples overview of many processes: one compact
panel per CTQ showing a density histogram with vertical spec-limit lines
and the target, plus the per-process Cpk annotated in the strip header.
This is the JMP "Process Capability Snapshot" — a Tufte-style dashboard for
spotting the worst CTQs at a glance without paging through individual
sixpacks.
}
