% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_capability.R
\name{plot_capability_histogram}
\alias{plot_capability_histogram}
\title{Capability Histogram with Dual Normal Curves}
\usage{
plot_capability_histogram(
  values,
  mean_val,
  sd_within,
  sd_overall,
  sd_between = NULL,
  specs = NULL,
  target = NULL,
  fitted_curve = NULL,
  bins = 30,
  subtitle_text = NULL,
  theme = NULL
)
}
\arguments{
\item{values}{Numeric vector of measurements.}

\item{mean_val}{Numeric scalar, process mean.}

\item{sd_within}{Numeric scalar, within-subgroup sigma (drives the within curve).}

\item{sd_overall}{Numeric scalar, overall sigma (drives the overall curve).}

\item{sd_between}{Optional numeric scalar, between-subgroup sigma. When
supplied (Between/Within analysis), a third normal curve is drawn in the
\code{danger} semantic color to visualize the between-subgroup variation.}

\item{specs}{Optional list with \code{lsl} and/or \code{usl}.}

\item{target}{Optional numeric scalar, target value.}

\item{fitted_curve}{Optional data.frame with columns \code{x} and \code{density} for a
non-normal fitted-distribution curve overlay.}

\item{bins}{Number of histogram bins. Defaults to 30.}

\item{subtitle_text}{Optional subtitle string override.}

\item{theme}{Theme spec.}
}
\value{
A ggplot object.
}
\description{
Draws the Panel-4 capability histogram: a density histogram, the within-σ
normal curve (solid) and the overall-σ normal curve (dashed), plus vertical
specification limits and the target line. For non-normal analyses an
optional fitted-distribution curve (Weibull / Lognormal / Gamma / Johnson)
can be overlaid via \code{fitted_curve}.
}
