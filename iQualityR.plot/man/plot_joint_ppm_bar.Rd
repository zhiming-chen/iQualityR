% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_capability_multivariate.R
\name{plot_joint_ppm_bar}
\alias{plot_joint_ppm_bar}
\title{Joint Yield & PPM Summary Bar}
\usage{
plot_joint_ppm_bar(
  yield_prob,
  ppm_expected,
  verdict,
  target_yield = 0.9973,
  theme = NULL
)
}
\arguments{
\item{yield_prob}{Numeric scalar, joint probability P(X in spec region),
between 0 and 1.}

\item{ppm_expected}{Numeric scalar, expected joint PPM (defects per million).}

\item{verdict}{Character, one of "pass", "watch", "fail". Controls bar color.}

\item{target_yield}{Numeric scalar, target yield threshold. Default 0.9973
(corresponds to a 3-sigma process).}

\item{theme}{Theme spec.}
}
\value{
A ggplot object.
}
\description{
Draws the multivariate capability summary panel: a horizontal yield bar
(joint probability P(X in spec region) as a percentage) with the 99.73\%
reference line, the expected PPM annotated, and the overall verdict
controlling the bar color. This replaces the previous monospaced text
summary card with an information-dense visual (industry consensus: text
panels lack visual quality; a yield bar with verdict coloring communicates
the same information at a glance).
}
