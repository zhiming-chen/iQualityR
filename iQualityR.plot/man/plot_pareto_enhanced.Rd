% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pareto_ok.R
\name{plot_pareto_enhanced}
\alias{plot_pareto_enhanced}
\title{Enhanced Pareto Chart}
\usage{
plot_pareto_enhanced(
  data,
  category_col = NULL,
  count_col = NULL,
  group_col = NULL,
  weight_col = NULL,
  threshold = 0.05,
  show_table = FALSE,
  theme = "academic",
  title = NULL,
  subtitle = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame, list, or named vector containing the data.}

\item{category_col}{Name of the category column (if data is a data.frame).}

\item{count_col}{Name of the count/value column (if data is a data.frame).}

\item{group_col}{Name of the grouping column for facetting (optional).}

\item{weight_col}{Name of the weight column for weighted analysis (optional).}

\item{threshold}{Threshold for grouping small categories into "Other" (default: 0.05).}

\item{show_table}{Whether to show a summary table below the chart (default: FALSE).}

\item{theme}{Theme to use (default: "academic").}

\item{title}{Chart title.}

\item{subtitle}{Chart subtitle.}

\item{...}{Additional arguments passed to ggplot2 functions.}
}
\value{
A ggplot object or a patchwork object (if show_table is TRUE).
}
\description{
Creates an enhanced Pareto chart with additional features like grouping,
weighted analysis, and interactive table display. Also supports facetting
and "Other" category aggregation.
}
\examples{
# From data.frame
data <- data.frame(
  defect = c("Scratch", "Dent", "Missing Part", "Color Issue", "Size Error", "Weight Issue"),
  count = c(45, 23, 15, 8, 5, 3)
)
plot_pareto_enhanced(data, "defect", "count")

\donttest{
# From named vector
counts <- c(Scratch = 45, Dent = 23, Missing = 15, Other = 7)
plot_pareto_enhanced(counts, show_table = TRUE)

# With grouping
grouped_data <- data.frame(
  defect = rep(c("Scratch", "Dent", "Missing Part"), 2),
  count = c(45, 23, 15, 30, 18, 10),
  period = rep(c("Before", "After"), each = 3)
)
plot_pareto_enhanced(grouped_data, "defect", "count", group_col = "period", show_table = TRUE)
}

}
