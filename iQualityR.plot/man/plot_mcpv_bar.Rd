% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_capability_multivariate.R
\name{plot_mcpv_bar}
\alias{plot_mcpv_bar}
\title{HPCI Three-Component Bar Chart (Shahriari 1995)}
\usage{
plot_mcpv_bar(
  npc,
  pv,
  lri,
  npc_threshold = 1,
  pv_threshold = 0.05,
  lri_threshold = 0.9,
  theme = NULL
)
}
\arguments{
\item{npc}{Numeric scalar, volume ratio component (>=0). Larger is better;
pass when \code{npc >= npc_threshold}.}

\item{pv}{Numeric scalar, Hotelling T^2 p-value for centering (0-1). Larger
is better (p large => fail to reject H0 => center on target); pass when
\code{pv >= pv_threshold}.}

\item{lri}{Numeric scalar, location ratio index (0-1). Larger is better;
pass when \code{lri >= lri_threshold}.}

\item{npc_threshold}{Numeric, pass threshold for npc. Default 1.0.}

\item{pv_threshold}{Numeric, pass threshold for pv. Default 0.05.}

\item{lri_threshold}{Numeric, pass threshold for lri. Default 0.9.}

\item{theme}{Theme spec.}
}
\value{
A ggplot object.
}
\description{
Draws the academic-standard horizontal three-component bar chart for the
HPCI (Shahriari-Hubele-Lawrence 1995) vector: npc (volume ratio),
pv (Hotelling T^2 centering p-value), and lri (location ratio index).
Each component is drawn as a horizontal bar with a threshold marker and
semantic coloring (pass/watch/fail). This is the industry-standard
visualization for multivariate capability vectors (Taam 1993 /
Shahriari 1995), replacing the previously used low-information radar
chart.
}
