% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DoeHelperFunctions.R
\name{taguchi_design}
\alias{taguchi_design}
\title{Taguchi Robust Design}
\usage{
taguchi_design(
  control_factors,
  noise_factors = NULL,
  response_vars = NULL,
  data = NULL,
  sn_type = "nominal",
  replication = 2,
  randomize = TRUE,
  seed = NULL,
  theme = "academic",
  auto_compute = TRUE
)
}
\arguments{
\item{control_factors}{List of control factor specifications.}

\item{noise_factors}{Optional list of noise factor specifications. When
\code{data} is supplied and contains columns matching the noise factor names,
the S/N ratio is computed per control-factor combination using the
noise-factor rows as replicates (inner/outer-array style). When \code{data}
is \code{NULL}, \code{noise_factors} is recorded on the returned object for
downstream use but does not generate an outer array.}

\item{response_vars}{Character vector of response variable names.}

\item{data}{Optional data frame containing the observed response values.
Must contain one row per run of the (replicated) design table, with
columns for every control factor and response variable declared on
\code{response_vars}. When supplied, ANOVA, S/N ratio, and contribution
analyses are produced and returned in \code{taguchi_analysis}.}

\item{sn_type}{Character scalar, the S/N ratio type: \code{"larger"},
\code{"nominal"} or \code{"smaller"}.}

\item{replication}{Integer scalar, number of replications (default 2, used
to estimate variation).}

\item{randomize}{Logical scalar, whether to randomize.}

\item{seed}{Optional integer scalar, the random seed.}

\item{theme}{Character scalar, the theme name.}

\item{auto_compute}{Logical scalar, whether to compute immediately.}
}
\value{
A list with elements \code{task} (the \code{IqrDoeTask}), \code{design} (the
design table) and \code{taguchi_analysis} (the S/N ratio analysis; \code{NULL}
when \code{data} is not supplied).
}
\description{
Creates and runs a Taguchi robust design. When response data
is supplied via the \code{data} argument, a Taguchi S/N ratio analysis and
ANOVA-based contribution analysis are also produced.
}
\examples{
\dontrun{
# Taguchi design (3 control factors)
control_factors <- list(
  list(name = "Temp", type = "continuous", levels = c(100, 150)),
  list(name = "Pressure", type = "continuous", levels = c(5, 10)),
  list(name = "Time", type = "continuous", levels = c(30, 60))
)

result <- taguchi_design(
  control_factors = control_factors,
  sn_type = "larger"
)

# View the S/N ratio analysis
print(result$taguchi_analysis)
}

}
