% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DoeHelperFunctions.R
\name{factorial_design}
\alias{factorial_design}
\title{Factorial Design}
\usage{
factorial_design(
  factors,
  design_type = "factorial",
  response_vars = NULL,
  replication = 1,
  center_points = 0,
  resolution = NULL,
  randomize = TRUE,
  seed = NULL,
  theme = "academic",
  auto_compute = TRUE
)
}
\arguments{
\item{factors}{List of factor specifications.}

\item{design_type}{Character scalar, \code{"factorial"} (full factorial) or
\code{"fractional"} (fractional factorial).}

\item{response_vars}{Character vector of response variable names.}

\item{replication}{Integer scalar, number of replications.}

\item{center_points}{Integer scalar, number of center points.}

\item{resolution}{Character scalar, the resolution (\code{"III"}, \code{"IV"} or
\code{"V"}), used only for fractional designs.}

\item{randomize}{Logical scalar, whether to randomize.}

\item{seed}{Optional integer scalar, the random seed.}

\item{theme}{Character scalar, the theme name.}

\item{auto_compute}{Logical scalar, whether to compute immediately.}
}
\value{
An \code{IqrDoeTask} object.
}
\description{
Creates and runs a full or fractional factorial experimental
design.
}
\examples{
\dontrun{
# Full factorial design
factors <- list(
  list(name = "A", type = "continuous", levels = c(-1, 1)),
  list(name = "B", type = "continuous", levels = c(-1, 1))
)

task <- factorial_design(factors = factors, design_type = "factorial")
}

}
