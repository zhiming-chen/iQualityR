% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DoeHelperFunctions.R
\name{orthogonal_design}
\alias{orthogonal_design}
\title{Orthogonal Design}
\usage{
orthogonal_design(
  factors,
  response_vars = NULL,
  replication = 1,
  randomize = TRUE,
  seed = NULL,
  theme = "academic",
  auto_compute = TRUE
)
}
\arguments{
\item{factors}{List of factor specifications; each element must contain
\code{name}, \code{type} and \code{levels}.}

\item{response_vars}{Character vector of response variable names.}

\item{replication}{Integer scalar, number of replications (default 1).}

\item{randomize}{Logical scalar, whether to randomize run order (default
\code{TRUE}).}

\item{seed}{Optional integer scalar, the random seed.}

\item{theme}{Character scalar, the theme name.}

\item{auto_compute}{Logical scalar, whether to compute the design
immediately (default \code{TRUE}).}
}
\value{
An \code{IqrDoeTask} object.
}
\description{
Creates and runs an orthogonal-array experimental design,
automatically generating the design table.
}
\examples{
\dontrun{
# 3-factor 2-level orthogonal design
factors <- list(
  list(name = "Temperature", type = "continuous", levels = c(100, 150)),
  list(name = "Pressure", type = "continuous", levels = c(5, 10)),
  list(name = "Time", type = "continuous", levels = c(30, 60))
)

task <- orthogonal_design(factors = factors)
task$summary()
task$plot()
}

}
