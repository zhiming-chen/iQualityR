% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DoeHelperFunctions.R
\name{predictive_doe_flywheel}
\alias{predictive_doe_flywheel}
\title{Predictive DOE Data Flywheel}
\usage{
predictive_doe_flywheel(
  historical_data,
  factor_cols,
  response_col,
  goal = "optimization",
  budget = "medium",
  industry = "manufacturing",
  seed = NULL
)
}
\arguments{
\item{historical_data}{Data frame of historical production / experiment
data (low fidelity).}

\item{factor_cols}{Character vector of factor / feature column names.}

\item{response_col}{Character scalar, the response variable column name.}

\item{goal}{Character scalar, the optimization goal: \code{"screening"} or
\code{"optimization"}.}

\item{budget}{Character scalar, the budget sensitivity: \code{"low"},
\code{"medium"} or \code{"high"}.}

\item{industry}{Character scalar, the industry type: \code{"manufacturing"} or
\code{"service"}.}

\item{seed}{Optional integer scalar, the random seed.}
}
\value{
A list with the results of each flywheel stage.
}
\description{
Implements the closed loop: historical data -> surrogate model -> DOE
recommendation -> new trials -> model update. This is the core capability
that distinguishes iQualityR from Minitab/JMP: historical data guides DOE,
and DOE results feed back into the predictive model.
}
\examples{
\dontrun{
# Scenario: you have 1000 historical production records and want to
# optimize the process parameters.
result <- predictive_doe_flywheel(
  historical_data = mes_history,
  factor_cols = c("Temp", "Pressure", "Time"),
  response_col = "Yield",
  goal = "optimization",
  budget = "medium"
)

# View the recommended minimum number of new trials
print(result$recommendation)

# Get the generated design table
print(result$doe_design)
}

}
