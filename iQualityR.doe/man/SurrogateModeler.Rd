% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SurrogateModeler.R
\name{SurrogateModeler}
\alias{SurrogateModeler}
\title{SurrogateModeler: Surrogate Model and Virtual Experiment Generator}
\description{
An R6 class that implements three families of low-cost simulated
experimental data generation:
\enumerate{
\item \strong{Parameterised response surface}: a mathematical model derived from
expert knowledge or the literature, combined with Monte Carlo noise.
\item \strong{Gaussian process surrogate}: a GP model trained on historical data
that predicts the response (and its uncertainty) at new parameter
settings.
\item \strong{Digital twin virtual DOE}: a complete DOE workflow executed in a
virtual environment as a substitute for physical experimentation.
}

\strong{Theoretical background}:
\itemize{
\item Santner, T. J., Williams, B. J., & Notz, W. I. (2018).
\emph{The Design and Analysis of Computer Experiments}.
\item The four DOE frontier references on surrogate modelling, digital
twinning and virtual experimentation provided by the user.
}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-SurrogateModeler-new}{\code{SurrogateModeler$new()}}
\item \href{#method-SurrogateModeler-simulate_parameterized}{\code{SurrogateModeler$simulate_parameterized()}}
\item \href{#method-SurrogateModeler-train_surrogate_gp}{\code{SurrogateModeler$train_surrogate_gp()}}
\item \href{#method-SurrogateModeler-predict_with_gp}{\code{SurrogateModeler$predict_with_gp()}}
\item \href{#method-SurrogateModeler-virtual_doe_experiment}{\code{SurrogateModeler$virtual_doe_experiment()}}
\item \href{#method-SurrogateModeler-get_builtin_model}{\code{SurrogateModeler$get_builtin_model()}}
\item \href{#method-SurrogateModeler-clone}{\code{SurrogateModeler$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SurrogateModeler-new"></a>}}
\if{latex}{\out{\hypertarget{method-SurrogateModeler-new}{}}}
\subsection{Method \code{new()}}{
Create a new SurrogateModeler instance.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{SurrogateModeler$new()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
A \code{SurrogateModeler} instance (invisibly).
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SurrogateModeler-simulate_parameterized"></a>}}
\if{latex}{\out{\hypertarget{method-SurrogateModeler-simulate_parameterized}{}}}
\subsection{Method \code{simulate_parameterized()}}{
Generate simulated experimental data from a parameterised
response surface. The supplied \code{response_model} is evaluated on the
design to obtain the noise-free ("true") response, to which Gaussian
measurement noise and (optionally) batch effects are added. The input
\code{design} data frame is never mutated; a copy is returned with the
additional result columns appended.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{SurrogateModeler$simulate_parameterized(
  design,
  response_model,
  noise_sd = 0,
  batch_effect = FALSE,
  n_batches = 3,
  batch_sd = 0,
  seed = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{design}}{Data frame of experimental settings (e.g. a CCD, LHS or
orthogonal array) generated by an upstream DOE planner.}

\item{\code{response_model}}{Function that takes a data frame and returns a
numeric vector of noise-free responses.}

\item{\code{noise_sd}}{Numeric. Standard deviation of the Gaussian measurement
noise (defaults to \code{0}, i.e. no noise).}

\item{\code{batch_effect}}{Logical. If \code{TRUE}, add a random batch effect to
each run.}

\item{\code{n_batches}}{Integer. Number of batches (used only when
\code{batch_effect = TRUE}).}

\item{\code{batch_sd}}{Numeric. Between-batch standard deviation (used only
when \code{batch_effect = TRUE}).}

\item{\code{seed}}{Random seed. When \code{NULL} the RNG state is left untouched.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A copy of \code{design} with the additional columns
\code{Response_Observed} (noisy response), \code{Response_True} (noise-free
response) and, when \code{batch_effect = TRUE}, \code{Batch} (batch index).
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SurrogateModeler-train_surrogate_gp"></a>}}
\if{latex}{\out{\hypertarget{method-SurrogateModeler-train_surrogate_gp}{}}}
\subsection{Method \code{train_surrogate_gp()}}{
Train a Gaussian process surrogate model on historical
data. All columns of \code{historical_data} other than \code{response_col} are
treated as factors. Leave-one-out (LOO) predictions are computed to
estimate the model's predictive error.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{SurrogateModeler$train_surrogate_gp(
  historical_data,
  response_col,
  kernel_type = "rbf"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{historical_data}}{Data frame containing both the factor columns
and the response column.}

\item{\code{response_col}}{Character. Name of the response column.}

\item{\code{kernel_type}}{Character. Covariance kernel: \code{"rbf"} (default) for
the radial basis function kernel or \code{"matern"} for the Matern kernel.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A list with elements \code{model} (the fitted \code{gausspr} object),
\code{factor_columns} (names of the factor columns), \code{n_training}
(number of training rows), \code{loo_rmse} (leave-one-out RMSE) and
\code{kernel_type} (the kernel that was used).
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SurrogateModeler-predict_with_gp"></a>}}
\if{latex}{\out{\hypertarget{method-SurrogateModeler-predict_with_gp}{}}}
\subsection{Method \code{predict_with_gp()}}{
Predict the response at new design points using a
previously trained GP surrogate. The prediction uncertainty is
approximated from the LOO RMSE stored on \code{gp_result}; optional
Gaussian noise can be added on top of the GP mean.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{SurrogateModeler$predict_with_gp(
  gp_result,
  new_design,
  add_noise = FALSE,
  noise_sd = NULL,
  seed = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{gp_result}}{Return value of \code{train_surrogate_gp()}.}

\item{\code{new_design}}{Data frame of new design points. Must contain the
factor columns referenced by \code{gp_result}.}

\item{\code{add_noise}}{Logical. If \code{TRUE}, add Gaussian noise of standard
deviation \code{noise_sd} to the GP mean.}

\item{\code{noise_sd}}{Numeric. Standard deviation of the added noise. When
\code{NULL} (default), the LOO RMSE of \code{gp_result} is used.}

\item{\code{seed}}{Random seed. When \code{NULL} the RNG state is left untouched.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A list with elements \code{predict} (numeric vector of predicted
responses), \code{se} (standard error), \code{lower} and \code{upper} (lower and
upper bounds of an approximate 95\% prediction interval).
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SurrogateModeler-virtual_doe_experiment"></a>}}
\if{latex}{\out{\hypertarget{method-SurrogateModeler-virtual_doe_experiment}{}}}
\subsection{Method \code{virtual_doe_experiment()}}{
Run a complete DOE workflow in a virtual environment.
The experimental design is generated internally, the supplied
\code{true_response_fn} plays the role of the digital twin, and Gaussian
measurement noise (and optionally batch effects) are added to its
output.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{SurrogateModeler$virtual_doe_experiment(
  n_factors,
  factor_ranges,
  true_response_fn,
  doe_method = "lhs",
  n_runs = NULL,
  n_center = 0,
  noise_sd = 0,
  batch_effect = FALSE,
  seed = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{n_factors}}{Integer. Number of factors in the design.}

\item{\code{factor_ranges}}{List. Each element is a list with \code{name},
\code{lower} and \code{upper} describing one factor.}

\item{\code{true_response_fn}}{Function. The digital-twin response function;
takes a design data frame and returns a numeric vector of responses.}

\item{\code{doe_method}}{Character. DOE method: \code{"ccd"} (central composite),
\code{"lhs"} (Latin hypercube, default) or \code{"full"} (full factorial at
two levels).}

\item{\code{n_runs}}{Integer. Number of runs (used by \code{"lhs"}). When \code{NULL},
defaults to \code{max(10 * n_factors, 20)}.}

\item{\code{n_center}}{Integer. Number of center points (used by \code{"ccd"}).}

\item{\code{noise_sd}}{Numeric. Standard deviation of the measurement noise.}

\item{\code{batch_effect}}{Logical. If \code{TRUE}, add a batch effect.}

\item{\code{seed}}{Random seed. When \code{NULL} the RNG state is left untouched.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A list with elements \code{design} (the generated design with
appended \code{Response_Observed}, \code{Response_True} and \code{RunOrder}
columns, plus \code{Batch} when \code{batch_effect = TRUE}), \code{n_runs},
\code{n_factors}, \code{doe_method}, \code{noise_sd} and \code{has_batch_effect}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SurrogateModeler-get_builtin_model"></a>}}
\if{latex}{\out{\hypertarget{method-SurrogateModeler-get_builtin_model}{}}}
\subsection{Method \code{get_builtin_model()}}{
Retrieve a pre-defined response model for quick testing
or teaching. These built-in models are single-response models: each
returns a single primary response as a numeric vector (not a list),
so they can be plugged directly into \code{simulate_parameterized()} or
\code{virtual_doe_experiment()}. For multi-response problems, supply
your own response function.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{SurrogateModeler$get_builtin_model(model_name)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{model_name}}{Character. Name of the built-in model: \code{"welding"}
(returns weld strength in MPa), \code{"injection"} (returns injection
moulding tensile strength in MPa) or \code{"battery"} (returns battery
capacity retention in \%).}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A function that takes a design data frame and returns a
numeric vector (the primary response of the selected model).
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-SurrogateModeler-clone"></a>}}
\if{latex}{\out{\hypertarget{method-SurrogateModeler-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{SurrogateModeler$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
