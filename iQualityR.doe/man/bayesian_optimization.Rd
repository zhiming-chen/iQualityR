% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DoeHelperFunctions.R
\name{bayesian_optimization}
\alias{bayesian_optimization}
\title{Bayesian Optimization Design}
\usage{
bayesian_optimization(
  factors,
  response_function,
  n_initial = NULL,
  n_iterations = 20,
  kernel = "rbf",
  minimize = TRUE,
  seed = NULL
)
}
\arguments{
\item{factors}{List of factor specifications.}

\item{response_function}{A black-box response function that takes a
data.frame and returns a numeric vector.}

\item{n_initial}{Integer scalar, number of initial LHS design points
(default \code{5 * n_factors}).}

\item{n_iterations}{Integer scalar, number of Bayesian-optimization
iterations (default 20).}

\item{kernel}{Character scalar, the kernel type: currently only \code{"rbf"}
is supported.}

\item{minimize}{Logical scalar, whether to minimize (default \code{TRUE}); set
to \code{FALSE} to maximize.}

\item{seed}{Optional integer scalar, the random seed.}
}
\value{
A list with the optimizer, design, responses, best point/value
and the optimization history.
}
\description{
Uses Bayesian optimization with a Gaussian-process surrogate
model to perform sequential experimental design, automatically
recommending the next optimal trial point.
}
\examples{
\dontrun{
# Define factors
factors <- list(
  list(name = "x1", type = "continuous", levels = c(0, 10)),
  list(name = "x2", type = "continuous", levels = c(0, 10))
)

# Define the response function (simulated)
response_fn <- function(df) {
  -(df$x1 - 5)^2 - (df$x2 - 3)^2 + 50
}

# Run Bayesian optimization
result <- bayesian_optimization(
  factors = factors,
  response_function = response_fn,
  n_iterations = 15
)

# View the best solution
print(result$best_point)
print(result$best_value)
}

}
