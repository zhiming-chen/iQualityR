% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_changepoint.R
\name{run_spc_changepoint}
\alias{run_spc_changepoint}
\title{Change-Point Detection Control Chart}
\usage{
run_spc_changepoint(
  data,
  measurement,
  cp_method = "pelt",
  cp_penalty = "MBIC",
  sigma_method = NULL,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{cp_method}{Method string. Default \code{"pelt"}.}

\item{cp_penalty}{Penalty string or numeric. Default \code{"MBIC"}.}

\item{sigma_method}{Sigma estimation method (default \code{"total"}).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results. The \code{data_tables}
field additionally contains a \code{change_points} data frame with columns
\code{index}, \code{mean_before}, \code{mean_after}, \code{mean_diff}.
}
\description{
Detects change-points in the process mean and/or variance
using a CUSUM-based binary segmentation algorithm. Change-points are
displayed as vertical lines on the time-series plot; segment means
before and after each change are reported.

If the optional \code{changepoint} package is installed, it will be used
(via \code{requireNamespace}) to apply the PELT algorithm with the
configured \code{cp_penalty}. Otherwise, an in-house binary segmentation
routine based on cumulative sums is used.
}
\examples{
set.seed(123)
df <- data.frame(measurement = c(rnorm(30, 100, 1),
                                 rnorm(30, 103, 1)))
\donttest{
task <- run_spc_changepoint(df, "measurement")
task$summary()
}
}
