% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_t2_mewma.R
\name{run_spc_t2_mewma}
\alias{run_spc_t2_mewma}
\title{T2 + MEWMA Hybrid Multivariate Control Chart}
\usage{
run_spc_t2_mewma(
  data,
  lambda = 0.2,
  nelson_rules = 1:8,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame with at least 2 numeric columns.}

\item{lambda}{MEWMA smoothing parameter in (0, 1]. Default 0.2.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Combines Hotelling T2 (sensitive to large mean shifts) with
MEWMA (sensitive to small sustained shifts) into a single chart.
Both statistics are computed on the same data; a violation is flagged
when EITHER statistic exceeds its respective upper control limit.

The combined statistic displayed on the chart is the maximum of
\code{T2 / UCL_T2} and \code{MEWMA / UCL_MEWMA} (a value > 1 indicates a
violation). Both raw statistics are stored in the \code{points} data table.
}
\examples{
set.seed(123)
df <- data.frame(
  x1 = c(rnorm(25, 50, 1), rnorm(10, 52, 1)),
  x2 = c(rnorm(25, 30, 0.8), rnorm(10, 31, 0.8))
)
\donttest{
task <- run_spc_t2_mewma(df, lambda = 0.2)
task$summary()
}
}
