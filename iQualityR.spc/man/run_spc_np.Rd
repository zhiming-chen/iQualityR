% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_attributes.R
\name{run_spc_np}
\alias{run_spc_np}
\title{NP Chart (Number Defective)}
\usage{
run_spc_np(
  data,
  count,
  sample_size,
  nelson_rules = 1:8,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing count and sample-size columns.}

\item{count}{Count of defective items (column name or numeric vector).}

\item{sample_size}{Sample size per subgroup (column name or numeric vector).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\description{
Creates an NP chart for monitoring the count of defective items
when sample sizes are equal (or approximately equal).
}
\examples{
set.seed(123)
df <- data.frame(
  defectives = rbinom(20, 50, 0.04),
  sample_size = rep(50, 20)
)
\donttest{
task <- run_spc_np(df, "defectives", "sample_size")
}
}
