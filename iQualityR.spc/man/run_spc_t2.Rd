% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_multivariate.R
\name{run_spc_t2}
\alias{run_spc_t2}
\title{Hotelling T2 Control Chart}
\usage{
run_spc_t2(
  data,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame with at least 2 numeric columns.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Creates a Hotelling T2 control chart for monitoring multiple
correlated variables simultaneously. The T2 statistic is the multivariate
generalization of the univariate t-statistic. Control limits use the
Phase I F-distribution approximation.
}
\examples{
set.seed(123)
df <- data.frame(
  x1 = rnorm(30, 50, 1),
  x2 = rnorm(30, 30, 0.8),
  x3 = rnorm(30, 75, 1.2)
)
\donttest{
task <- run_spc_t2(df)
task$summary()
}
}
