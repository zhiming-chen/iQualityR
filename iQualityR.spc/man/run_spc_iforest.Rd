% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_ml.R
\name{run_spc_iforest}
\alias{run_spc_iforest}
\title{Isolation Forest Anomaly Detection Control Chart}
\usage{
run_spc_iforest(
  data,
  iforest_ntree = 200L,
  iforest_sample_size = 256L,
  iforest_threshold = 0.6,
  sigma_method = NULL,
  nelson_rules = 1:8,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame with at least 1 numeric column.}

\item{iforest_ntree}{Integer. Number of isolation trees. Default 200.}

\item{iforest_sample_size}{Integer. Subsample size per tree. Default 256.}

\item{iforest_threshold}{Numeric. Anomaly score threshold in (0, 1).
Default 0.6.}

\item{sigma_method}{Sigma method (default \code{"total"}).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Detects anomalies in multivariate data using an Isolation
Forest. The algorithm isolates observations by random recursive partitioning;
anomalous points require fewer splits to isolate.

\strong{Backend selection}: If the \code{isotree} package is available, a fast C++
implementation is used. Otherwise, an in-house R implementation of
isolation forest is used. The active backend is reported in
\code{task$results$diagnostics$backend}.

\strong{Explainability}: Per-feature contribution proxies are stored in
\code{task$results$data_tables$feature_contributions}.
}
\examples{
set.seed(123)
df <- data.frame(
  x1 = c(rnorm(40, 50, 1), rnorm(10, 55, 1)),
  x2 = c(rnorm(40, 30, 0.8), rnorm(10, 33, 0.8))
)
\donttest{
task <- run_spc_iforest(df, iforest_ntree = 100, iforest_threshold = 0.55)
task$summary()
}
}
