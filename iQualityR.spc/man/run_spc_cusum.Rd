% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_time_weighted.R
\name{run_spc_cusum}
\alias{run_spc_cusum}
\title{CUSUM Control Chart (Tabular)}
\usage{
run_spc_cusum(
  data,
  measurement,
  sigma_method = NULL,
  k = 0.5,
  h = 4.77,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{sigma_method}{Sigma estimation method (default \code{"mr_bar"}).}

\item{k}{Reference value (in sigma units). Default 0.5 (detects 0.5 sigma shifts).}

\item{h}{Decision interval (in sigma units). Default 4.77 (ARL0 ~ 370).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Creates a tabular (two-sided) Cumulative Sum control chart.
The CUSUM statistic accumulates deviations from the target, making it
sensitive to small persistent shifts. Reference value \code{k} (in sigma units)
and decision interval \code{h} (in sigma units) control sensitivity.
}
\examples{
set.seed(123)
df <- data.frame(measurement = c(rnorm(20, 50, 1), rnorm(10, 51.5, 1)))
\donttest{
task <- run_spc_cusum(df, "measurement", k = 0.5, h = 4.77)
}
}
