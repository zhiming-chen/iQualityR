% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_arima_resid.R
\name{run_spc_arima_resid}
\alias{run_spc_arima_resid}
\title{ARIMA Residual Control Chart}
\usage{
run_spc_arima_resid(
  data,
  measurement,
  arima_order = c(1L, 0L, 1L),
  sigma_method = NULL,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{arima_order}{Integer vector \code{c(p, d, q)}. Default \code{c(1, 0, 1)}.}

\item{sigma_method}{Sigma estimation method for residuals (default \code{"mr_bar"}).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments passed to \code{stats::arima}.}
}
\value{
An \code{IqrSpcTask} object with computed results. The \code{diagnostics}
field additionally stores the ARIMA model coefficients, sigma2, and
AIC for traceability.
}
\description{
Fits an ARIMA(p,d,q) model to the time series, then applies
an I-MR control chart to the residuals. Useful for autocorrelated
data where standard Shewhart charts produce misleading limits.

The model is fit via \code{stats::arima(x, order = c(p, d, q))}.
Residuals are extracted and monitored via \code{iQualityR.stat}'s
moving-range sigma estimation and Nelson rules detection.
}
\examples{
set.seed(123)
# Autocorrelated process
ar1 <- stats::arima.sim(list(ar = 0.6), n = 60, sd = 1) + 100
df <- data.frame(measurement = as.numeric(ar1))
\donttest{
task <- run_spc_arima_resid(df, "measurement", arima_order = c(1, 0, 0))
task$summary()
}
}
