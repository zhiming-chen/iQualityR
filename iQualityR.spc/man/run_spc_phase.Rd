% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_phase.R
\name{run_spc_phase}
\alias{run_spc_phase}
\title{SPC Phase Analysis}
\usage{
run_spc_phase(
  data,
  measurement,
  chart_type = "imr",
  phase_boundaries,
  sigma_method = NULL,
  subgroup_size = NULL,
  nelson_rules = 1:8,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{chart_type}{Chart type string (default \code{"imr"}).}

\item{phase_boundaries}{Integer vector of indices where phases start.
For example, \code{c(15, 30)} splits data into phases \code{1-14}, \code{15-29}, \code{30-n}.}

\item{sigma_method}{Sigma method (default NULL uses chart-specific default).}

\item{subgroup_size}{Subgroup size (default NULL uses chart-specific default).}

\item{nelson_rules}{Nelson rules enabled (default 1:8).}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments passed to \code{SpcPlan}.}
}
\value{
A list of \code{IqrSpcTask} objects, one per phase.
}
\description{
Performs phase (stage) analysis on a control chart. The data is split at
specified boundaries and control limits are computed separately for each
phase. Useful for assessing whether a process improvement has shifted the
process mean or reduced variability.
}
\examples{
set.seed(123)
df <- data.frame(measurement = c(rnorm(20, 100, 1), rnorm(15, 102, 1)))
\donttest{
phases <- run_spc_phase(df, "measurement", "imr",
  phase_boundaries = c(21))
length(phases)  # 2 phases
}
}
