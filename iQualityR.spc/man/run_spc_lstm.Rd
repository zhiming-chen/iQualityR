% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_ml.R
\name{run_spc_lstm}
\alias{run_spc_lstm}
\title{LSTM Anomaly Detection Control Chart}
\usage{
run_spc_lstm(
  data,
  measurement,
  lstm_units = 32L,
  lstm_window = 10L,
  lstm_epochs = 20L,
  lstm_batch_size = 16L,
  lstm_threshold = 3,
  sigma_method = NULL,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{lstm_units}{Integer. LSTM units per layer. Default 32.}

\item{lstm_window}{Integer. Lookback window size. Default 10.}

\item{lstm_epochs}{Integer. Training epochs (keras only). Default 20.}

\item{lstm_batch_size}{Integer. Training batch size. Default 16.}

\item{lstm_threshold}{Numeric. Anomaly threshold (z-score scale). Default 3.}

\item{sigma_method}{Sigma method (default \code{"total"}).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results. The \code{diagnostics}
field contains \code{ai_diagnostic} with method, anomaly_score, shap_values,
rule_attribution, confidence, and backend.
}
\description{
Detects anomalies in a univariate time series using a Long
Short-Term Memory (LSTM) neural network. The model is trained to predict
the next value from a sliding window of past observations; large
prediction errors are flagged as anomalies.

\strong{Backend selection}: If the \code{keras} (and \code{tensorflow}) packages are
available, a true LSTM is trained. Otherwise, the function falls back to
an ARIMA(1,0,1)+EWMA combination that approximates the LSTM behavior
without external dependencies. The active backend is reported in
\code{task$results$diagnostics$backend}.

\strong{Explainability}: The \code{ai_diagnostic} field contains the anomaly score,
SHAP-like contribution (reconstruction error), rule attribution, and
confidence.
}
\examples{
set.seed(123)
df <- data.frame(measurement = c(rnorm(50, 100, 1),
                                  rnorm(10, 105, 1)))
\donttest{
task <- run_spc_lstm(df, "measurement", lstm_window = 5, lstm_epochs = 5)
task$summary()
}
}
