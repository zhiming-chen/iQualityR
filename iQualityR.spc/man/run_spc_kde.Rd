% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_kde.R
\name{run_spc_kde}
\alias{run_spc_kde}
\title{KDE Nonparametric Control Chart}
\usage{
run_spc_kde(
  data,
  measurement,
  kde_bandwidth = NULL,
  sigma_method = NULL,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{kde_bandwidth}{Optional numeric bandwidth. If NULL (default),
Sheather-Jones (\code{bw.SJ}) is used to select the bandwidth automatically.}

\item{sigma_method}{Sigma estimation method (default \code{"total"}).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results. The \code{diagnostics}
field stores the bandwidth used.
}
\description{
Creates a control chart whose limits are derived from the
empirical distribution of the data via Kernel Density Estimation
(KDE). Useful when the underlying process is non-normal and the
standard 3-sigma limits of Shewhart charts are not appropriate.

The density is estimated via \code{stats::density(x, bw, kernel = "gaussian")}.
Control limits are the empirical 0.135th and 99.865th percentiles
(equivalent to +-3 sigma under normality) computed from the KDE
via quantile integration.
}
\examples{
set.seed(123)
# Non-normal data (exponential)
df <- data.frame(measurement = rexp(50, rate = 0.1))
\donttest{
task <- run_spc_kde(df, "measurement")
task$summary()
}
}
