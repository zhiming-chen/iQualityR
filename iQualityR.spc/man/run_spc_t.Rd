% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_rare_events.R
\name{run_spc_t}
\alias{run_spc_t}
\title{T Chart (Time Between Events)}
\usage{
run_spc_t(
  data,
  measurement,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Column of time intervals between events.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Creates a T chart for monitoring the time between consecutive
rare events. The T chart is appropriate when defects are infrequent and
the time interval is exponentially distributed. A log transform is applied
internally so that Nelson rules can be applied on the normal scale.
}
\examples{
set.seed(123)
df <- data.frame(time_between = rexp(30, rate = 0.1))
\donttest{
task <- run_spc_t(df, "time_between")
}
}
