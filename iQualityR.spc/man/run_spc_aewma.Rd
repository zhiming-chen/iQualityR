% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_adaptive.R
\name{run_spc_aewma}
\alias{run_spc_aewma}
\title{Adaptive EWMA Control Chart}
\usage{
run_spc_aewma(
  data,
  measurement,
  sigma_method = NULL,
  aewma_lambda = 0.2,
  aewma_k = 1.5,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{sigma_method}{Sigma estimation method (default \code{"mr_bar"}).}

\item{aewma_lambda}{Base EWMA smoothing in (0, 1]. Default 0.2.}

\item{aewma_k}{Sensitivity parameter (>0). Larger = faster adaptation. Default 1.5.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Creates an Adaptive EWMA (AEWMA) control chart where the
smoothing parameter \code{lambda} varies over time based on the magnitude
of the recent forecast error. Large deviations increase \code{lambda},
making the chart more responsive to sudden shifts while retaining
the smoothing benefit of standard EWMA in steady state.

The adaptive update is:
\eqn{\lambda_i = \lambda_0 + (1 - \lambda_0) \cdot \min(1, k \cdot |e_i| / \sigma)}
where \eqn{e_i = x_i - z_{i-1}} is the one-step forecast error.
}
\examples{
set.seed(123)
df <- data.frame(measurement = c(rnorm(30, 100, 1), rnorm(15, 103, 1)))
\donttest{
task <- run_spc_aewma(df, "measurement", aewma_lambda = 0.2, aewma_k = 1.5)
task$summary()
}
}
