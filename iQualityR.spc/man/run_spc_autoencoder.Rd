% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_ml.R
\name{run_spc_autoencoder}
\alias{run_spc_autoencoder}
\title{Autoencoder Anomaly Detection Control Chart}
\usage{
run_spc_autoencoder(
  data,
  ae_encoding_dim = 2L,
  ae_hidden_dim = 8L,
  ae_epochs = 50L,
  ae_batch_size = 16L,
  ae_threshold_quantile = 0.99,
  sigma_method = NULL,
  nelson_rules = 1:8,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame with at least 1 numeric column.}

\item{ae_encoding_dim}{Integer. Bottleneck dimension. Default 2.}

\item{ae_hidden_dim}{Integer. Hidden layer dimension. Default 8.}

\item{ae_epochs}{Integer. Training epochs (keras only). Default 50.}

\item{ae_batch_size}{Integer. Training batch size. Default 16.}

\item{ae_threshold_quantile}{Numeric. Quantile for threshold in (0, 1).
Default 0.99.}

\item{sigma_method}{Sigma method (default \code{"total"}).}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Detects anomalies in multivariate data using an Autoencoder
neural network. The model compresses input to a low-dimensional latent
representation and reconstructs it; large reconstruction errors are
flagged as anomalies.

\strong{Backend selection}: If \code{keras} is available, a deep autoencoder is
trained. Otherwise, the function falls back to PCA-based reconstruction
error, which approximates a linear autoencoder without external
dependencies. The active backend is reported in
\code{task$results$diagnostics$backend}.

\strong{Explainability}: Per-feature reconstruction errors are stored in
\code{task$results$data_tables$feature_contributions} and the \code{ai_diagnostic}
list contains SHAP-like attribution.
}
\examples{
set.seed(123)
df <- data.frame(
  x1 = c(rnorm(40, 50, 1), rnorm(10, 55, 1)),
  x2 = c(rnorm(40, 30, 0.8), rnorm(10, 33, 0.8))
)
\donttest{
task <- run_spc_autoencoder(df, ae_encoding_dim = 1, ae_epochs = 10)
task$summary()
}
}
