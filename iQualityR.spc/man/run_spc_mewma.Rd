% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_multivariate.R
\name{run_spc_mewma}
\alias{run_spc_mewma}
\title{MEWMA Control Chart}
\usage{
run_spc_mewma(
  data,
  lambda = 0.2,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame with at least 2 numeric columns.}

\item{lambda}{MEWMA smoothing parameter in (0, 1]. Default 0.2.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Creates a Multivariate Exponentially Weighted Moving Average
(MEWMA) control chart (Lowry et al., 1992). MEWMA is the multivariate
extension of EWMA and is sensitive to small sustained shifts in the
process mean vector.
}
\examples{
set.seed(123)
df <- data.frame(
  x1 = c(rnorm(20, 50, 1), rnorm(10, 51.5, 1)),
  x2 = c(rnorm(20, 30, 0.8), rnorm(10, 31.2, 0.8))
)
\donttest{
task <- run_spc_mewma(df, lambda = 0.2)
}
}
