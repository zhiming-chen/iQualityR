% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_time_weighted.R
\name{run_spc_ewma}
\alias{run_spc_ewma}
\title{EWMA Control Chart}
\usage{
run_spc_ewma(
  data,
  measurement,
  sigma_method = NULL,
  lambda = 0.2,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{sigma_method}{Sigma estimation method (default \code{"mr_bar"}).}

\item{lambda}{EWMA smoothing parameter in (0, 1]. Default 0.2.
Smaller values detect smaller shifts but react slower.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Creates an Exponentially Weighted Moving Average (EWMA)
control chart. The EWMA statistic \eqn{Z_i = \lambda X_i + (1 - \lambda) Z_{i-1}}
smooths short-term fluctuations, making it sensitive to small sustained
shifts in the process mean.
}
\examples{
set.seed(123)
df <- data.frame(measurement = c(rnorm(20, 100, 1), rnorm(10, 102, 1)))
\donttest{
task <- run_spc_ewma(df, "measurement", lambda = 0.2)
task$summary()
}
}
