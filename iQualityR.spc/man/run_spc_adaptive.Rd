% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_adaptive.R
\name{run_spc_adaptive}
\alias{run_spc_adaptive}
\title{Adaptive Rolling-Window Control Chart}
\usage{
run_spc_adaptive(
  data,
  measurement,
  sigma_method = NULL,
  window_size = 20L,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{sigma_method}{Sigma estimation method (default \code{"mr_bar"}).}

\item{window_size}{Rolling window size (>=5). Default 20.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\value{
An \code{IqrSpcTask} object with computed results.
}
\description{
Creates an adaptive control chart whose center line and
sigma are recomputed over a rolling window of the most recent
\code{window_size} observations. Useful for processes with naturally
drifting mean where fixed limits would produce false alarms.

For each point \code{i}, the center is the mean of the previous
\code{window_size} observations (including point \code{i}) and sigma is
estimated via \code{iQualityR.stat::sigma_estimate} on the same window.
}
\examples{
set.seed(123)
df <- data.frame(measurement = c(rnorm(30, 100, 1),
                                 rnorm(20, 102, 1)))
\donttest{
task <- run_spc_adaptive(df, "measurement", window_size = 15)
task$summary()
}
}
