% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spc_run_variables.R
\name{run_spc_imr_rs}
\alias{run_spc_imr_rs}
\title{I-MR-R/S Control Chart}
\usage{
run_spc_imr_rs(
  data,
  measurement,
  subgroup = NULL,
  subgroup_size = 5L,
  sigma_method = NULL,
  nelson_rules = 1:8,
  phase_boundaries = NULL,
  conf_level = 0.95,
  theme = "academic",
  ...
)
}
\arguments{
\item{data}{Data frame containing the measurement column.}

\item{measurement}{Measurement column name.}

\item{subgroup}{Optional subgroup column name. If NULL, observations are
chunked into groups of \code{subgroup_size}.}

\item{subgroup_size}{Subgroup size (default 5). Must be >= 2.}

\item{sigma_method}{Sigma estimation method. Default uses \code{"r_bar"}.}

\item{nelson_rules}{Nelson rule numbers enabled (default 1:8).}

\item{phase_boundaries}{Optional integer vector of phase boundary indices.}

\item{conf_level}{Confidence level (default 0.95).}

\item{theme}{Theme name or IqrTheme object.}

\item{...}{Additional arguments.}
}
\description{
Creates an I-MR-R/S (Individual-Moving Range / Range or Std Dev)
chart. The between-subgroup dispersion is estimated separately from
within-subgroup variation. Sigma uses pooled SD by default.
}
\examples{
set.seed(123)
df <- data.frame(
  measurement = rnorm(40, mean = 50, sd = 1.2),
  subgroup = rep(1:8, each = 5)
)
\donttest{
task <- run_spc_imr_rs(df, "measurement", subgroup = "subgroup",
                      subgroup_size = 5)
}
}
