# iQualityR 项目通用要求（General Requirements）

> 版本：v1.1  日期：2026-08-02  状态：生效中
> 范围：`iQualityR` meta 包及全部 10 个成员子包（.core / .plot / .stat / .msa / .capa / .doe / .sampling / .reliability / .predict / .spc）
> 适用对象：后续所有开发、测试、文档编写、代码审查工作
> 维护方式：本文件为单一事实来源（single source of truth），后续如需更新通用要求，直接修改本文件并 bump 版本号，无需另建新文档。

## 修订记录

| 版本 | 日期 | 更新内容 | 维护人 |
|---|---|---|---|
| v1.0 | 2026-08-02 | 首版，综合 VIGNETTE_PLAN.md / STAT_ANALYSIS_PLAN.md / OPTIMIZATION_TODO.md / 沟通记录形成 | iQualityR Team |
| v1.1 | 2026-08-02 | 新增第 7 节"跨子包变更管理"（条目 39-40），后续章节顺延，附录检查清单补充跨子包变更核对项 | iQualityR Team |

---

## 1. 整体架构与依赖（Architecture & Dependencies）

1. **monorepo 结构**：10 个子包 + 1 个 meta 包；meta 包 `iQualityR` 自动 attach 全部成员包（遵循 tidyverse 约定）。
2. **依赖单向**：`.core ← .stat ← {功能子包}`；`.stat` 禁止 Imports `.plot`，需要绘图时通过 `requireNamespace("iQualityR.plot")` 软依赖（列为 Suggests）。
3. **R 版本**：>= 4.1.0；新代码优先使用原生 `|>` 管道；已存在的 magrittr `%>%` 暂保留，不强制改写（R1-2 已确认不实施）。
4. **内核不膨胀**：`.stat` 只承载基础统计 + 质量度量 + SPC 基础设施；多元分析 / 时间序列 / 可靠性 / MSA / DOE 各自独立子包承载，`.stat` 只向它们提供 L0 原语。

## 2. 分层架构 L0–L3（Layered Architecture）

5. **四层分治**：
   - **L0 原语层**：无状态纯函数，提供常数 / 注册表 / 验证，不绘图不报告不缓存状态。
   - **L1 引擎层**：R6 Analyzer，纯统计计算，返回 `stat_result`，不调用 ggplot、不写文件、不 print。
   - **L2 表现层**：Plotter / Reporter / Interpreter，调用 `.plot` 与 `.core`，不重做统计计算。
   - **L3 入口层**：R6 整合器 + 便捷函数，编排 L1 + L2，不实现新统计算法。
6. **层职责严格隔离**：L1 不绘图不写文件不 print；L2 不重做统计计算；L3 不实现新算法。
7. **板块自包含**：每板块一个 `R/<domain>/` 目录，自包含引擎 + 表现 + 入口 + 测试 + 文档；板块间只通过 L0 原语或显式 `::` 调用耦合，禁止相互私有访问。

## 3. R6 基类与契约（R6 Base Classes & Contracts）

8. **统一基类**：所有 Analyzer 继承 `IqrAnalyzerBase`，Plotter 继承 `IqrPlotterBase`，Reporter 继承 `IqrReporter`，Task 继承 `IqrTaskBase`，Plan 继承 `IqrPlanBase`。
9. **命名契约**：

   | 类别 | 规范 | 示例 |
   |---|---|---|
   | R6 Analyzer | `<Domain>Analyzer` | `HTestAnalyzer` |
   | R6 Plotter | `<Domain>Plotter` | `HTestPlotter` |
   | R6 Reporter | `<Domain>Reporter` | `HTestReporter` |
   | R6 整合器 | `iqr_<domain>` | `iqr_htest` |
   | 便捷函数 | `<domain>_<verb>` | `htest_run` |
   | L0 原语函数 | snake_case 动词 | `fit_distribution` / `get_d2` |
   | 私有方法 | `.<verb>` | `.compute_prob` |
   | 文件名 | R6 类 PascalCase，功能 snake_case | `HTestAnalyzer.R` / `dist_fit.R` |
   | 目录名 | kebab-case | `R/htest/` / `R/sample-size/` |
   | 测试文件 | `test-<domain>.R` | `test-htest.R` |
   | vignette | `<domain>.Rmd` | `htest.Rmd` |

10. **签名契约（固定，不得擅自变更）**：
    - L2 Plotter：`$plot(result, plot_type = "auto", show_table = FALSE, theme_obj = NULL)`
    - L2 Reporter：`$report(result, format = c("data.frame","console","excel"), path = NULL, audience = "manager")`
    - L2 Interpreter：`$interpret(result, audience = c("manager","technical","client"))`
    - L3 整合器：`$new(theme="academic")` / `$run(...)` / `$plot(plot_type="auto", show_table=FALSE, theme_obj=NULL)` / `$interpret(audience="manager")` / `$report(format="data.frame", path=NULL, audience="manager")` 五方法
    - L3 便捷函数：`<domain>_run / _plot / _interpret / _report` 四函数
11. **返回契约**：所有 L1 Analyzer 返回 `stat_result`（轻量 S3，`class = c("stat_result", "<domain>_result")`），必填字段 `domain / test_type / method`；检验类还需 `statistic / parameter / p.value / conf.int / estimate / alternative`；非检验类（描述统计/区间估计/样本量）遵循类似 list 结构，`domain / method / estimate` 必填，`statistic / p.value` 可选。
12. **app_result 协议**：面向前端 / API 的结果使用 `app_success / app_error`，结构为 `ok / code / message / data / tables / plots / files / warnings / metadata`，必须 JSON 可序列化。

## 4. 可视化通用要求（Visualization / Plot）

13. **统一主题系统**：所有绘图必须通过 `IqrTheme` 主题管线取色，**禁止硬编码**十六进制颜色（如 `#1F77B4`）或命名颜色（如 `steelblue` / `red`）；颜色全部来自 `IqrPlotterBase` 的 `.pal_*` / `.scale_*` 工具箱。
14. **默认主题统一为 `"academic"`**：
    - 所有函数 `theme` 参数默认值均为 `"academic"`。
    - 所有 vignette setup 块设置 `options(iqr.default_theme = "academic")`。
    - 禁止默认使用 `prism` / `tech` / `workbench`（仅主题切换演示章节可显式传参）。
    - 用户可通过 `options(iqr.default_theme = "xxx")` 或显式传参切换。
15. **四分类调色板**（按视觉任务匹配）：
    - **discrete（离散/分类）**：因子水平、分组 —— 通过 `colorRampPalette` 自动扩展。
    - **sequential（连续/幅度）**：密度、计数、风险评分。
    - **diverging（发散/带符号）**：相关系数、残差、偏差。
    - **semantic（语义/判定）**：pass / fail / watch / good / bad / neutral。
16. **WCAG 无障碍**：文本与背景对比度遵循 WCAG 2.x，使用 `.contrast_text(bg)` 自动选择黑/白字色；需考虑色盲安全。
17. **图层复用**：通过 `.plot` 的 `layers_*` 函数（`layers_control_chart` / `layers_spec_limits` / `layers_qq` / `layers_histogram_density` 等）复用绘图逻辑，避免各子包重复实现。
18. **主题对象传递（Pattern A）**：统一通过 `theme = self$theme_obj` 将 `IqrTheme` 对象传给 `.plot` 函数，不传字符串主题名（除用户层入口可接受字符串）。
19. **`.iqr_plotter` 单例**：每子包 `package.R` 中定义一次 `IqrPlotterBase` 单例，避免重复初始化与样板代码。
20. **Excel 报告配色跟随主题**：`ExcelExporter` 的表头 / 条纹 / 边框颜色与活动主题一致。
21. **树形 / 流程图用正交折线**：fishbone / turtle / 决策树等图使用方角水平垂直线（orthogonal polylines），**不用斜线**。
22. **交互式可视化优先**：拒绝纯 SVG / 静态图方案；面向 Web / dashboard 的可视化须提供交互式方案（plotly / htmlwidgets 等）。

## 5. Vignette 文档规范（Vignette Standards）

23. **统一 YAML 头**：

    ```yaml
    ---
    title: "Chapter Title"
    author: "iQualityR.<subpackage> Team"
    date: "`r Sys.Date()`"
    output:
      rmarkdown::html_vignette:
        toc: true
        toc_depth: 3
    vignette: >
      %\VignetteIndexEntry{Chapter Title}
      %\VignetteEngine{knitr::rmarkdown}
      %\VignetteEncoding{UTF-8}
    ---
    ```

24. **单一 setup 块**（MSA 风格，`include=FALSE`）：

    ```r
    knitr::opts_chunk$set(
      collapse = TRUE, comment = "#>",
      fig.width = 8, fig.height = 5, fig.align = "center",
      warning = FALSE, message = FALSE
    )
    library(iQualityR.<subpackage>)
    library(ggplot2)
    fmt_num <- function(x, digits = 4) formatC(x, format = "f", digits = digits)
    fmt_pct <- function(x, digits = 2) paste0(formatC(x, format = "f", digits = digits), "%")
    ```

25. **理论优先（Theory first）**：每个统计 / 专业 vignette 必须先讲理论基础、原理、公式，再给代码；纯 API 参考才可省略。
26. **质量场景驱动（Quality scenarios）**：所有可执行示例必须使用真实质量工程场景（制造 / 制药 / 服务 / 半导体等），禁止纯伪代码演示。
27. **统一章节结构**：`Introduction → Theory → API/Function → Quality Scenario → Decision Guide → Parameter Reference → Summary → Further Reading`。
28. **书籍连贯性（Book coherence）**：vignette 顺序阅读如书章，相互交叉引用；聚合形成 *"Quality Management with R: The iQualityR Framework"*。
29. **核心方法论**：meta 包与 `.core` 必须清晰阐述 `Plan → Task → Analysis → Summary/Plot → Report` 统一工作流（4M1E 元数据模型），作为全系列设计哲学基石。
30. **vignette 默认主题**：setup 块设置 `options(iqr.default_theme = "academic")`，绘图调用省略 `theme=`；仅在主题切换演示章节显式传参。
31. **vignette 语言为英文**（CRAN 合规要求）；中文注释 / roxygen 须翻译为英文。

## 6. 代码规范（Code Standards）

32. **Roxygen 文档**：每个导出函数必须含 `@title / @description / @param / @return / @examples`；`@examples` 使用真实质量场景，**避免 `\dontrun{}`**，改用 `requireNamespace` 包裹可选依赖。
33. **错误处理**：错误用 `stop("...", call. = FALSE)`；输入验证只在系统边界（用户输入 / 外部 API）做，内部代码信任契约，不为不可能场景加防御。
34. **防御性编程清理**：移除 `exists() + ::` 冗余防御模式；依赖在 Imports 中的包直接用 `::` 或 importFrom。
35. **命名风格**：函数 / 变量 snake_case；R6 类 PascalCase；私有方法 `.<verb>`；常量 UPPER_SNAKE。
36. **C++ 加速**：通过 Rcpp，**必须有纯 R fallback**；`RcppExports.R` 放在标准位置（非 `R/sigma/` 等子目录）。
37. **代码注释语言**：路径注释（`# File: R/xxx.R`）、函数描述用**英文**；禁止中文注释（CRAN 合规）。
38. **依赖清理**：DESCRIPTION 中 Imports 只列真正需要的包；冗余 importFrom 清理；移除未使用的 Suggests（如 memoise / SuppDists）。

## 7. 跨子包变更管理（Cross-Package Change Management）

39. **禁止直接修改他子包函数**：当本子包函数调用其他子包函数时，若发现被调用函数需要修改（改签名 / 改行为 / 改返回值 / 改默认参数等），**不得直接修改**被调用函数。必须先记录存在的问题与变更原因（写入提交说明、issue 或临时调研笔记），再按条目 40 的流程推进。
40. **修改前必须调查全项目调用情况**：在修改任何被跨子包调用的函数之前，必须详细调查本项目中所有对该函数的调用点 —— 包括 R 源码（`R/`）、测试（`tests/`）、vignette（`vignettes/`）、文档（`man/` `.Rd`）、示例数据脚本（`data-raw/`）—— 评估变更影响范围，确认不会与其他调用方产生冲突（签名不兼容、返回结构变化、默认值改变等）后，方可实施修改。调查结果与变更方案应记录在提交说明或 issue 中，作为变更依据留存。

    **执行步骤**：
    1. 用 `Grep` / `SearchCodebase` 检索全项目对该函数的调用（函数名、`::` 调用、`importFrom`）。
    2. 逐个核对调用点是否会被拟变更影响（参数顺序、参数名、返回值结构、副作用）。
    3. 若存在冲突风险，优先选择**向后兼容**方案（新增参数带默认值、新增返回字段而非删除）；确需破坏性变更须 bump 版本并同步更新所有调用方。
    4. 变更实施后，回归运行受影响子包的测试套件，确认无破坏。

## 8. 测试要求（Testing Standards）

41. **覆盖率目标**：>= 85%（`.stat` 已达 89.9%，1759 个测试，作为基准）。
42. **测试文件组织**：按板块拆分 `test-<domain>.R`，每板块独立测试文件，禁止超大单文件。
43. **测试用例覆盖四类**：输入验证 / 功能验证 / 边界条件 / 错误路径。
44. **CRAN 兼容**：`R CMD check --as-cran` 无 ERROR、无 WARNING（网络相关 WARNING 除外）、无 NOTE。
45. **测试稳定性**：用 `set.seed()` 保证可重复；不依赖网络；不依赖外部文件绝对路径；测试数据自包含或来自 `inst/extdata/`。

## 9. 国际化与受众（i18n & Audience）

46. **i18n 支持**：通过 `iqr_t() / iqr_locale() / iqr_set_locale()` 支持 `en-US` / `zh-CN`；消息字典存放于 `inst/i18n/`。
47. **三受众模板**：Reporter / Interpreter 须支持三种受众输出：
    - `manager`（管理层）：结论导向，业务语言。
    - `technical`（技术人员）：含统计细节、自由度、假设条件。
    - `client`（客户）：业务语言，弱化统计术语。

## 10. 质量工程领域规范（Quality Engineering Domain）

48. **4M1E 元数据模型**：Plan 层统一捕获 Man / Machine / Material / Method / Environment 上下文，跨子包共享。
49. **标准对齐**：
    - MSA 对齐 AIAG MSA-4 第 4 版（2024）。
    - 过程能力对齐 ISO 22514。
    - 控制图对齐 ASTM E2587。
50. **SPC 动态更新**：SPC 支持动态、周期性数据更新（持续数据录入），非一次性分析。
51. **源码忠实**：当有参考源码 / 算法参数时，实现必须忠实于源码方案；如发现源码问题，须显式提出，不得擅自偏离。

## 11. 版本与发布（Versioning & Release）

52. **L0 原语是稳定 API**：`.stat` 的 L0 原语（常数 / sigma 估计 / 分布拟合 / 质量指标）对功能子包是稳定 API，签名变更必须 bump 版本号。
53. **License**：MIT + file LICENSE，每子包独立 LICENSE 文件。
54. **meta 包安装**：通过 `remotes::install_github("zhiming-chen/iQualityR", subdir = "iQualityR")`；`iQualityR/DESCRIPTION` 的 `Remotes:` 字段指向各子包 subdir，自动按依赖顺序安装。

---

## 附录 A：检查清单（Code Review / PR Checklist）

提交 PR 或代码审查时，逐项核对：

- [ ] 新代码遵循 L0–L3 分层，未越界（L1 不绘图、L2 不算统计、L3 不写新算法）
- [ ] R6 类继承正确基类，命名符合契约
- [ ] Plotter / Reporter / Interpreter 签名与契约 2 一致
- [ ] L1 Analyzer 返回 `stat_result`，字段齐全
- [ ] 无硬编码颜色，全部走 `IqrTheme` 工具箱
- [ ] 默认 `theme = "academic"`，未用 prism/tech/workbench 作默认
- [ ] Roxygen 含完整 `@title/@description/@param/@return/@examples`
- [ ] `@examples` 无 `\dontrun{}`，可选依赖用 `requireNamespace` 包裹
- [ ] 无中文注释 / 中文 roxygen（CRAN 合规）
- [ ] 新增测试覆盖输入验证 / 功能 / 边界 / 错误路径四类
- [ ] `R CMD check --as-cran` 通过（无 ERROR/WARNING/NOTE）
- [ ] vignette YAML / setup 块 / 章节结构符合规范
- [ ] vignette 理论优先，示例用真实质量场景
- [ ] 依赖方向正确（.stat 不 Imports .plot）
- [ ] 无冗余防御代码（`exists() + ::`）
- [ ] 跨子包函数变更前已调查全项目调用点（R/tests/vignettes/man/data-raw），确认无冲突；优先向后兼容方案

## 附录 B：参考文档

- `packages/VIGNETTE_PLAN.md` —— Vignette 总体规划（10 子包章节蓝图）
- `packages/iQualityR.stat/STAT_ANALYSIS_PLAN.md` —— .stat 架构契约 v2.0
- `packages/OPTIMIZATION_TODO.md` —— 跨子包主题迁移与优化清单
- `packages/README.md` —— meta 包安装与使用说明
- `packages/iQualityR.capa/过程能力分析 (CapabilityAnalysis) 开发需求规划 v5.0.md` —— 领域需求文档范例
