% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_functions.R
\name{sampling_multiple}
\alias{sampling_multiple}
\title{Multiple Sampling Plan Analysis}
\usage{
sampling_multiple(
  stages,
  aql = 0.01,
  rql = 0.1,
  alpha = 0.05,
  beta = 0.1,
  data = NULL,
  theme = "academic"
)
}
\arguments{
\item{stages}{List of stage plans; each element must have \code{n} and \code{c}.}

\item{aql}{Numeric. AQL (default 0.01).}

\item{rql}{Numeric. RQL (default 0.10).}

\item{alpha}{Numeric. Producer's risk target (default 0.05).}

\item{beta}{Numeric. Consumer's risk target (default 0.10).}

\item{data}{Optional data frame for actual sampling validation.}

\item{theme}{Theme name (default \code{"academic"}).}
}
\value{
An \link{IqrSamplingTask} object with results already computed.
}
\description{
Build a multi-stage plan and run OC curve, risk, and ASN analysis.
}
\examples{
\donttest{
task <- sampling_multiple(stages = list(
  list(n = 20, c = 0),
  list(n = 20, c = 1),
  list(n = 20, c = 2)
))
task$summary()
}
}
