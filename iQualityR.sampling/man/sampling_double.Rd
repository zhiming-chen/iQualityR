% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sampling_functions.R
\name{sampling_double}
\alias{sampling_double}
\title{Double Sampling Plan Analysis}
\usage{
sampling_double(
  stage1,
  stage2,
  aql = 0.01,
  rql = 0.1,
  alpha = 0.05,
  beta = 0.1,
  data = NULL,
  theme = "academic"
)
}
\arguments{
\item{stage1}{List with \code{n} (sample size) and \code{c} (acceptance number),
and optionally \code{r} (rejection number). If \code{r} is missing, it defaults
to \code{c + 1}.}

\item{stage2}{List with \code{n} and \code{c}.}

\item{aql}{Numeric. AQL (default 0.01).}

\item{rql}{Numeric. RQL (default 0.10).}

\item{alpha}{Numeric. Producer's risk target (default 0.05).}

\item{beta}{Numeric. Consumer's risk target (default 0.10).}

\item{data}{Optional data frame for actual sampling validation.}

\item{theme}{Theme name (default \code{"academic"}).}
}
\value{
An \link{IqrSamplingTask} object with results already computed.
}
\description{
Build a two-stage sampling plan and run OC curve, risk, and ASN analysis.
}
\examples{
\donttest{
task <- sampling_double(
  stage1 = list(n = 32, c = 0, r = 2),
  stage2 = list(n = 32, c = 1)
)
task$summary()
}
}
